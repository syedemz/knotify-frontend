/**
 * ChatRoomScreen — placeholder for the per-friend message thread.
 *
 * The real implementation ships in story 15.5 and replaces this file.
 * This placeholder satisfies the `ChatStack` route registration requirement
 * from story 15.4 without pulling in any unimplemented UI.
 *
 * @module features/chat/screens/ChatRoomScreen
 */

import React from 'react';

/**
 * Placeholder for the chat room screen (story 15.5).
 *
 * Renders `null` — intentionally invisible. Story 15.5 replaces this
 * with the real WhatsApp-style thread + composer implementation.
 */
export function ChatRoomScreen(): React.ReactElement | null {
  return null;
}
