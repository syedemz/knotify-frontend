# knotify-frontend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Not yet started. Awaiting `architecture.md` and `/create-plan`.

## Active blockers
None. Phase 1 gates §17.22 and §17.26 both resolved on 2026-07-19.

## Before shipping

Local auth mock mode is active while the AWS backend is unspun (to save cost during UI walkthrough). Before the first real production build, delete the mock scaffolding in this order:

1. **`src/config/env.ts`** — delete the `isMockAuth` field from the exported `env` object (currently keyed off the sentinel `cognito.userPoolId === 'eu-central-1_dummydummy'`).
2. **`src/services/auth/cognitoClient.ts`** — delete every `if (env.isMockAuth) { ... }` branch (currently in `ensureConfigured`, `signUp`, `confirmSignUp`, `signIn`, `signOut`, `refreshSession`) and delete the `makeMockTokens` helper.
3. **`src/config/backendConfig.dev.json`** — replace the dummy sentinel values (`eu-central-1_dummydummy`, `dummydummydummydummydummy1`, `dummy.example.invalid`, `dummy.appsync-api...`, `dummy.cloudfront.net`) with the real values emitted by the backend Terraform outputs. `env.ts` throws at import time on any `<FILL_*>` placeholder, so partial fills are safe.

After all three are done, `grep -r isMockAuth src/` should return zero hits and `grep -r dummydummy src/config/` should return zero hits. Add a CI grep gate at that time if you want a hard stop.

### Mock-only registration + profile pipeline (added phase 11, 2026-08-03)

Registration completion and the phase-12 profile pipeline are intentionally mock-only until the backend (Cognito + AWS API + DB) is stood up. Every temporary surface added by phases 11–12 is grep-able via `TODO(mock-only)`. Before the first real production build, remove them in this order:

1. **Wire up `custom:profile_complete` JWT claim decode in `src/state/auth/AuthProvider.tsx`.** Resolve the `TODO(phase-2)` in the `profileComplete` `useMemo` (lines ~122-129) — decode the ID token, read the `custom:profile_complete` claim, return the boolean. This is what actually unlocks AppTabs post-onboarding once real Cognito is in play.
2. **Verify Cognito refresh returns a fresh ID token with the updated claim.** `AuthProvider.refresh()` calls `cognitoClient.refreshSession()`. If Amplify caches the ID token instead of re-fetching from Cognito after the backend flips `profile_complete_verified`, the claim will stay stale — in which case we need a manual `/oauth2/token` call or a sign-out/sign-in round-trip after the final PATCH. Verify with a real Cognito user pool before removing the mock-only bypass.
3. **Delete `src/state/onboardingCompletion/`** (the `OnboardingCompletionProvider` and its context) and remove its mount from `App.tsx`.
4. **Delete the additive `dummyOnboardingComplete` check in `src/navigation/RootNavigator.tsx`** — restore to a pure `profileComplete`-driven gate.
5. **Delete the mock dispatch branch in `src/services/api/httpClient.ts`** (the `if (env.isMock) { … mockRequest … }` at the top of `request()`) and **delete `src/services/api/mocks/mockRequest.ts`**. Once removed, real HTTP client calls the real backend for every path. Note: this replaces the earlier MSW-based mock (see 2026-08-04 change) — MSW was retired on Hermes because `msw/native` depends on browser globals that would require fragile polyfills.
6. **Delete the secure-store snapshot logic in `Page31FaceCaptureScreen`** — the `dummy.profile` write and the `dummy.onboarding.complete` flag write on mock-submit success.
7. **Delete the secure-store read logic in the mock GET `/profile/me` handler** and the merge logic in the mock PATCH `/profile/me` handler.
8. **One-shot migration on next launch:** wipe secure-store keys `dummy.profile` and `dummy.onboarding.complete` so no stale local data lingers.
9. **Verify YES/NO backend casing.** Fields `father_retired`, `mother_retired` (TEXT columns per phase 7) and `has_children`, `move_abroad` (BOOL columns per phase 8, already coerced) are written as literal `"YES"` / `"NO"` (uppercase) in the draft. Confirm the real backend accepts this exact casing for the two TEXT columns, or update `src/config/options/yesNo.json` (single source of truth) and re-run affected screens.

After all of the above: `grep -r 'TODO(mock-only)' src/` should return zero hits.

10. **Replace `chatRoomExistsForUser` in `src/features/chat/navigation/openChatRoom.ts`** with a real check (AsyncStorage lookup + backend query). Delete the `TODO(mock-only)` grep-tag once the check is real. See `openChatRoom.ts` module JSDoc for the full replacement spec.

### Naming debt to clean up when the last dummy fixture ships

Both items below are pure naming — the code works correctly for real backend data today. Rename when the mock-only surfaces are gone so future readers aren't confused by stale names.

1. **`resolveDummyPhoto()`** in `src/assets/dummyPhotoRegistry.ts`. Already handles real remote URIs correctly (pass-through as `{ uri }` when the path is not a registered bundled asset). Once the deck / friend / request fixtures are wiped, rename to `resolvePhotoSource()` (or inline at callsites) and delete the bundled-asset lookup map. Callsites: `src/components/Avatar.tsx`, `src/features/landing/components/CandidateHero.tsx`, `src/navigation/AppTabs.tsx` (Menu tab icon).

## Follow-up tickets

- **Real face-in-oval bounds check on Page 31.** The frame processor in `Page31FaceCaptureScreen.tsx` currently passes `faces.length > 0` as `faceInsideOval` — i.e. "any face detected anywhere in the frame", not "face bounds intersect the oval". Auto-capture threshold is bumped to 150 frames (~5s @ 30fps) as a stop-gap so the manual shutter button remains the practical capture path. Proper fix: read face bounds from `detectFaces()`, translate camera-sensor coordinates → screen coordinates (accounting for preview scaling, letterboxing, and front-camera mirroring), and gate `onFrame(true)` on real intersection with the `FaceOvalOverlay`'s bounds. Verify on ≥2 device sizes before shipping. Revert `CONSECUTIVE_FRAMES_REQUIRED` back to 15 once implemented.

## Critical design decisions
None yet.

## Recent changes
- 2026-08-27: Bug-fix commit on feat/phase-15-friend-req-chat-shell: (1) CollapsingTabBar in AppTabs.tsx now returns null when Chat tab's nested-focused route is ChatRoomScreen, eliminating the tab-bar overlay that occluded the composer; hooks moved above the conditional return to satisfy Rules of Hooks. (2) New shared helper `src/features/chat/navigation/openChatRoom.ts` — exports `chatRoomExistsForUser` (TODO(mock-only), always true) and `useOpenChatRoom` hook; OtherProfileScreen.handleChatPress now calls `openChatRoom(profile.user_id)` instead of showing a "coming soon" snackbar; MyProfileScreen.handleSayHi now delegates to `openChatRoom(MEHVISH_USER_ID)` instead of inlining navigate. Removed `otherProfile.chat.comingSoonToast` from labels.en.json + labels.ur.json (only callsite deleted). Tests: +20 new (6 openChatRoom + 6 AppTabs CollapsingTabBar + 2 OtherProfileScreen AC(l) + 1 MyProfileScreen.editTab AC(h) update + 3 MyProfileScreen.editTab AC(h) new assertions + 2 AppTabs existing re-wired). 2606 → 2624 passing (3 pre-existing failures unchanged). tsc: zero errors. eslint: zero new errors (3 pre-existing unused-disable warnings unchanged).
- 2026-08-27: Story 15.7 complete (commit bda6647, branch feat/phase-15-friend-req-chat-shell). RequestAcceptedModal + MyProfileScreen Edit-tab dev trigger: added `src/features/friendRequests/components/RequestAcceptedModal.tsx` (presentation-only, props: visible/profile/onClose/onSayHi; backdrop→onClose, Say hi→onSayHi+onClose, Not now→onClose; returns null when profile===null or !visible; interpolated title "You and {{firstName}} are now connected!" via `.replace('{{firstName}}', profile.first_name ?? '')` pattern; same Modal+backdrop+card shell as IncomingRequestModal — kept independent, no RequestModalCard extraction made since RequestAcceptedModal has fewer card items and diverges enough; grep-tagged TODO(mock-only)); updated `src/features/profile/screens/MyProfileScreen.tsx` (imported RequestAcceptedModal + MEHVISH_USER_ID from ChatProvider; added acceptedModalProfile useState; handleOpenAcceptedModal/handleCloseAcceptedModal resolve Mehvish via getFullProfile(MEHVISH_USER_ID); handleSayHi calls navigation.navigate('Chat', { screen: 'ChatRoomScreen', params: { friendUserId: MEHVISH_USER_ID } }) — host-owned cross-tab nav (B1 contract); second Button sibling child in DevTriggersPanel testID=dev-trigger-accepted-btn; RequestAcceptedModal screen-level mount; nav type widened to AppTabsParamList to type-check cross-tab navigate; removed unused useEffect import); added 5 friendRequests.accepted.* labels + acceptedButtonLabel to EN+UR with full parity. Non-obvious decisions: (a) No RequestModalCard extraction — the two modals share the same backdrop/card shell pattern but card content differs enough (6 items vs 4; no age/city/aboutMe in RequestAcceptedModal) that extraction would save ~10 lines while adding an abstraction layer; rule-of-two applied; (b) navigation type widened to AppTabsParamList (not intersected with MenuLocalParamList) — goBack() is available on all nav objects regardless of param list; (c) `first_name ?? ''` guard required because DummyFullProfile.first_name inherits `string | null` from UserProfile. Tests: 13 RequestAcceptedModal + 8 new MyProfileScreen.editTab = 21 new; 2585 → 2606 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. eslint: zero errors (8 pre-existing unused-disable warnings unchanged). Tracking issue #133 closed.
- 2026-08-27: Phase 15 (Friend requests + chat UI shell) complete. All 7 stories shipped: 15.1 Requests AsyncStorage helper + FriendshipProvider.sendRequest wiring (dummy.requests.outgoing key, outgoingRequestIds/sendRequest/hasOutgoingRequest surface, wired at MarriageLandingScreen + BookmarkDeckViewScreen callsites), 15.2 ChatHistory AsyncStorage helper + Mehvish seed JSON + BGDark asset move (ChatMessage type, 18-message chatMehvish.json, chatHistoryStorage helper), 15.3 ChatProvider + useChatHistory hook (MEHVISH_USER_ID constant, per-friend Map cache, seed hydration, sendMessage/updateMessage/deleteMessage), 15.4 ChatStack + ChatListScreen + ChatListRow + formatChatTimestamp (pure no-deps timestamp helper, WhatsApp-style friends list, Rules-of-Hooks-compliant row), 15.5 ChatRoomScreen + MessageBubble (tiled bgDark.jpg ImageBackground, inverted FlatList, pill composer with Mic↔Send toggle, status icons, WhatsApp tail corner, cross-tab navigation validated), 15.6 IncomingRequestModal + DevTriggersPanel + first dev trigger (slot-based panel, Qurat fixture, snackbar host pattern), 15.7 RequestAcceptedModal + second dev trigger (Mehvish fixture, host-owned Say-hi cross-tab navigation). Phase-wide non-obvious patterns: (a) all mock surfaces grep-tagged TODO(mock-only) — full teardown checklist in teardown_additions block of PRD and Before shipping section of context.md; (b) ChatProvider version-counter pattern: Map<friendUserId, number> in useState signals re-renders when cacheRef (useRef) mutates without re-rendering the full tree; (c) Rules-of-Hooks compliance was a recurring concern — ChatListRow declares useChatHistory at the component top level (not inside FlatList.renderItem); ChatRoomScreen + MyProfileScreen declare all hooks before conditional returns; (d) B1 contract (host owns navigation + provider mutations) applied consistently across both modals — modal fires onSayHi/onAccept/onDecline, host owns navigate + acceptRequest + declineRequest; (e) MessageBubble uses testID='bubble-sent'|'bubble-received' on the outer View (not on lucide SVG nodes which don't forward testID to native); status icon wrapped in View with testID='status-icon-sent|delivered|read' for same reason; (f) formatChatTimestamp uses local midnight comparisons against new Date() primitives — no date-fns/luxon dependency. Total test delta across phase 15: 2360 (end of phase 14) → 2606 (end of phase 15) = +246 tests. PR open: feat/phase-15-friend-req-chat-shell → development.
- 2026-08-27: Story 15.6 complete (branch feat/phase-15-friend-req-chat-shell). IncomingRequestModal + DevTriggersPanel + MyProfileScreen Edit-tab dev trigger: added `src/features/friendRequests/components/IncomingRequestModal.tsx` (presentation-only, props: visible/profile/onClose/onAccept/onDecline; backdrop dismisses on tap; Accept→onAccept+onClose, Decline→onDecline+onClose; "View full profile" → onClose then cross-tab navigate to Explore/OtherProfileScreen; returns null when profile===null or !visible; all hooks declared before conditional returns per Rules of Hooks; Avatar initials/about_me accessed with null-guard + unknown cast; grep-tagged TODO(mock-only)); added `src/features/profile/components/DevTriggersPanel.tsx` (slot-based panel, optional title/description props override defaults, children rendered in Column gap="sm", root View with testID="dev-triggers-panel"; grep-tagged TODO(mock-only)); updated `src/features/profile/screens/MyProfileScreen.tsx` (replaced "Coming soon" EmptyState on Edit tab with DevTriggersPanel + Button child (testID="dev-trigger-incoming-btn"); modal host pattern — MyProfileScreen owns acceptRequest/declineRequest calls + local Snackbar(3s) via useState<string|null>; snackbar uses .replace('{firstName}', value) pattern since t() has no interpolation; QURAT_USER_ID constant from require dummyqurat.json at module scope); added 7 friendRequests.incoming.* labels + 3 menu.myProfile.editTab.devTriggers.* labels to EN+UR with full parity (new keys merged into existing "menu" object — duplicate top-level key bug caught and fixed). Tests: 16 IncomingRequestModal + 7 DevTriggersPanel + 13 MyProfileScreen.editTab + updated 21 MyProfileScreen = 57 new/updated tests; all passing. tsc: zero errors. Tracking issue #132 closed.
- 2026-08-27: Story 14.1 complete (branch feat/phase-14-bookmarks). BookmarksStorage helper + BookmarksProvider + useBookmarks hook: added `src/features/bookmarks/storage/bookmarksStorage.ts` (zero-React AsyncStorage helper, exports `getBookmarks`, `saveBookmarks`, `addBookmark`, `removeBookmark`, `isBookmarked`, `clearBookmarks`, `BOOKMARKS_STORAGE_KEY = 'dummy.bookmarks'`; fail-open JSON parse with console.warn; grep-tagged TODO(mock-only)); added `src/state/bookmarks/BookmarksProvider.tsx` (in-memory mirror hydrated once on mount, `useBookmarks()` hook exposing bookmarks/loading/addBookmark/removeBookmark/isBookmarked/getBookmark; grep-tagged TODO(mock-only)); mounted `BookmarksProvider` in `App.tsx` between `FriendshipProvider` and `NavigationContainer`. Tests: 16 storage + 10 provider = 26 new tests; 2360 → 2398 pass. tsc: zero errors. Tracking issue #122 closed.
- 2026-08-27: Story 14.3 complete (commit 69e778a, branch feat/phase-14-bookmarks). Bookmarks subtab in ExploreHomeScreen + BookmarkCard 2-column grid: added `src/features/bookmarks/components/BookmarkCard.tsx` (portrait 3:4 card, full-bleed RN Image via resolveDummyPhoto(), flat rgba(0,0,0,0.55) scrim, Heading+Text overlay, accent.primary 3px border, shadows.sm, whole-card Pressable with accessibilityRole=button); extended ExploreHomeScreen to three subtabs (Friends | Requests | Bookmarks — added handleSelectBookmarks, renderBookmarkCard, key props on all FlatLists to prevent numColumns invariant violation on tab switch); added `BookmarkDeckViewScreen: { userId: string }` to ExploreStackParamList in types.ts; created `BookmarkDeckViewScreenPlaceholder.tsx` co-located in src/features/bookmarks/screens/; registered route in ExploreStack.tsx; added explore.bookmarks.{tab,emptyTitle,emptyDescription} to labels.en.json + labels.ur.json with full parity. Tests: 7 BookmarkCard (ACs a–e all pass) + 7 new ExploreHomeScreen Bookmarks-tab ACs (h–l) = 27 new tests; all 20 ExploreHomeScreen tests pass; 3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged. tsc: zero errors. expo-linear-gradient NOT in package.json. Tracking issue #124 closed.
- 2026-08-27: Story 15.1 complete (branch feat/phase-15-friend-req-chat-shell). Requests AsyncStorage helper + FriendshipProvider.sendRequest wiring: added `src/features/friendRequests/storage/requestsStorage.ts` (zero-React AsyncStorage helper, exports `getOutgoingRequests`, `saveOutgoingRequests`, `addOutgoingRequest`, `removeOutgoingRequest`, `hasOutgoingRequest`, `clearOutgoingRequests`, `OUTGOING_REQUESTS_STORAGE_KEY = 'dummy.requests.outgoing'`; fail-open JSON parse; grep-tagged TODO(mock-only)); extended `src/state/friendship/FriendshipProvider.tsx` with outgoing-requests mirror — `outgoingRequestIds: readonly string[]`, `sendRequest(userId): Promise<void>`, `hasOutgoingRequest(userId): boolean`; mirror hydrated via `useEffect` + `getOutgoingRequests()` on mount (cancellable); `sendRequest` writes AsyncStorage then updates in-memory mirror (idempotent dedup in both layers); no changes to friends/requests/acceptRequest/declineRequest surfaces. Wired `SendRequestModal.onConfirmed` at both callsites: `MarriageLandingScreen.handleRequestConfirmed` now calls `sendRequest(currentDeck.user_id).catch(console.warn)` before `animateDeckTransition(1)` + snackbar; `BookmarkDeckViewScreen.handleConfirmed` calls `sendRequest(profile.user_id).catch(console.warn)` before `navigation.goBack()`. Both callsites preserve existing `TODO(mock-only)` comments. Updated `BookmarkDeckViewScreen.test.tsx` to add `useFriendship` mock (screen now calls the hook; test previously only mocked `useBookmarks`). Non-obvious decisions: (a) `useEffect` uses `cancelled` flag guard to prevent stale state update on fast unmount; (b) `setOutgoingRequestIds` callback checks `prev.includes(userId)` to avoid duplicating in-memory mirror even if AsyncStorage write races; (c) `FriendshipProvider.test.tsx` upgraded `beforeEach` to `async` + `await AsyncStorage.clear()` to isolate outgoing-request storage between AC(p) remount tests. Tests: 16 requestsStorage + 6 new FriendshipProvider (AC l/m/n/o/p) = 22 new; 2433 → 2455 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. eslint: zero new errors (3 pre-existing unused-directive warnings in MarriageLandingScreen unchanged). Tracking issue #127 closed.
- 2026-08-27: Story 14.4 complete (branch feat/phase-14-bookmarks). BookmarkDeckViewScreen + FloatingAddRequestButton + SendRequestModal wiring: added `src/features/bookmarks/screens/BookmarkDeckViewScreen.tsx` (route param via useRoute, resolves profile from useBookmarks().getBookmark(userId), EmptyState on missing bookmark, BackHeaderBar + Animated.ScrollView + DeckCard, FloatingAddRequestButton FAB, SendRequestModal sibling, handleConfirmed calls goBack() — TODO(mock-only): real request-create ships in phase 15, bookmark NOT removed on confirm); added `src/features/bookmarks/components/FloatingAddRequestButton.tsx` (exact mirror of FloatingChatButton geometry — TAB_BAR_HEIGHT=49, BUTTON_SIZE=60, UserPlus icon, tabBarHidden shared value, verbatim-copy pattern); updated ExploreStack.tsx to use real BookmarkDeckViewScreen, deleted BookmarkDeckViewScreenPlaceholder.tsx; added bookmarks.deckView.{back,sendRequest,sendRequestAccessibility,missingTitle,missingDescription} to labels.en.json + labels.ur.json; scrollHandler wired to tabBarHidden for continuous collapse across stack screens; tab-stickiness confirmed via native-stack (ExploreHomeScreen stays mounted across push/pop, useState preserved). Tests: 7 FloatingAddRequestButton + 10 BookmarkDeckViewScreen + 3 ExploreStack extensions = 20 new tests; 2398 → 2433 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. SendRequestModal testIDs (send-request-card, send-request-no-ask) were pre-existing — no passive additions needed. Tracking issue #125 closed.
- 2026-08-27: Phase 14 (Bookmarks) complete. All 4 stories shipped: 14.1 BookmarksStorage helper + BookmarksProvider + useBookmarks hook (AsyncStorage, dummy.bookmarks key, in-memory mirror), 14.2 Star button → bookmark toggle on MarriageLandingScreen (add/remove with snackbar, CollapsingActionBar isSuperLikeActive visual state), 14.3 Bookmarks subtab in ExploreHomeScreen + BookmarkCard 2-column grid (portrait 3:4, flat scrim, route type + placeholder), 14.4 BookmarkDeckViewScreen + FloatingAddRequestButton + SendRequestModal wiring (real screen replaces placeholder, tab-stickiness preserved via native-stack). Tracking issues #122-#125 closed. PR opened (feat/phase-14-bookmarks → development).
- 2026-08-27: Story 15.2 complete (commit 4d25d4d, branch feat/phase-15-friend-req-chat-shell). ChatHistory AsyncStorage helper + Mehvish seed JSON + BGDark asset move: copied BGDark.jpg from workspace animations folder into `assets/chat/bgDark.jpg` (new directory; source path not referenced in any code — ChatRoomScreen (15.5) will import via project-relative require); added `src/types/ChatMessage.ts` (readonly interface with id/sender/text/timestamp/status/'sent'|'delivered'|'read'/editedAt?; timestamps are unix milliseconds); added `assets/dummychat/chatMehvish.json` (exactly 18 seed messages, matrimonial-context small talk between Adnan and Mehvish, top-level shape `{ _meta: { base_timestamp_ms, description }, messages: [...] }`, covers all three status states, both senders, one editedAt, timestamps spanning ~3 days); added `src/features/chat/storage/chatHistoryStorage.ts` (zero React imports, exports `CHAT_HISTORY_KEY_PREFIX = 'dummy.chat.'`, `chatHistoryKey`, `getHistory`, `saveHistory`, `appendMessage` (idempotent by id), `updateMessage` (patch on found id; no-op+warn on miss), `deleteMessage` (hard delete; idempotent), `clearHistory`; fail-open JSON parse; grep-tagged TODO(mock-only)). Non-obvious decisions: (a) seed hydration deliberately excluded from the storage helper — lives in ChatProvider (15.3) so each layer can be tested independently; (b) hard delete chosen for mock simplicity; phase 17 swaps for soft-delete semantics when real backend arrives; (c) `_meta` block stays in the fixture file only and is never persisted to AsyncStorage — provider reads `seedJson.messages`. Tests: 22 chatHistoryStorage + 13 chatHistoryFixtures = 35 new tests; 2455 → 2490 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. eslint: zero new errors. Tracking issue #128 closed.
- 2026-08-27: Story 15.5 complete (branch feat/phase-15-friend-req-chat-shell). ChatRoomScreen (WhatsApp-style thread + composer) + MessageBubble: replaced null-rendering ChatRoomScreen placeholder with full implementation — `KeyboardAvoidingView` root (padding/height per platform), safe-area header with ArrowLeft back, `ProfileThumbnailCircle size=32`, `Heading variant="heading.sm" numberOfLines={1}`, MoreVertical triple-dot no-op (testID='chat-room-menu-btn'); `ImageBackground` tiled dark chat texture (`assets/chat/bgDark.jpg`) wrapping inverted FlatList rendering `MessageBubble` components; pill-shaped composer with `RNTextInput` (testID='chat-room-input') and circular trailing button toggling between Mic (empty) and Send (non-empty) icons; EmptyState guard when `getFullProfile()` returns null/undefined; ALL hooks declared before conditional return (Rules of Hooks compliance). Added `src/features/chat/components/MessageBubble.tsx` — outermost View carries testID='bubble-sent'|'bubble-received', WhatsApp tail-corner via `borderBottomRightRadius: theme.radii.sm` (sent) / `borderBottomLeftRadius: theme.radii.sm` (received), footer row with HH:mm timestamp + '· edited' suffix when editedAt set + `StatusIcon` (sent only) wrapping lucide Check/CheckCheck in `<View testID="status-icon-sent|delivered|read">` (critical: lucide SVGs don't forward testID to native nodes). Added 8 label keys `chat.room.*` to labels.en.json + labels.ur.json (full parity). Tests: 8 MessageBubble + 12 ChatRoomScreen + 2 ChatStack.crossTab integration = 26 new; 2522 → 2548 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). Cross-tab navigation test uses a test-local MenuCrossTabStack to verify `navigation.navigate('Chat', { screen: 'ChatRoomScreen', params: { friendUserId } })` end-to-end without jest.fn() spies. tsc: zero errors. eslint: zero errors. Tracking issue #131 closed.
- 2026-08-27: Story 15.4 complete (branch feat/phase-15-friend-req-chat-shell). ChatStack + ChatListScreen + ChatListRow + formatChatTimestamp: added `src/navigation/ChatStack.tsx` (createNativeStackNavigator with ChatListScreen initial + ChatRoomScreen placeholder returning null, headerShown: false — mirrors ExploreStack/MenuStack pattern); added `ChatStackParamList` to `src/navigation/types.ts` (ChatListScreen: undefined, ChatRoomScreen: { friendUserId: string }); updated `AppTabsParamList.Chat` from `undefined` to `NavigatorScreenParams<ChatStackParamList>`; replaced Chat-tab EmptyState placeholder in `AppTabs.tsx` with `ChatStack`; added `src/features/chat/screens/ChatListScreen.tsx` (header "Chat" via chat.list.headerTitle with safe-area top, FlatList of friends from useFriendship(), renderItem returns standalone ChatListRow, EmptyState on zero friends); added `src/features/chat/components/ChatListRow.tsx` (standalone component — useChatHistory at TOP LEVEL per Rules-of-Hooks; View-based layout with ProfileThumbnailCircle size=48 + flex:1 text column (name Heading.heading.sm + preview Text.body.sm) + trailing timestamp Text.label.sm; last-message preview: non-empty → last message text, loading/empty → chat.list.noMessagesYet; TouchableArea calls onPress; interpolated accessibilityLabel from chat.list.rowAccessibility); added `src/features/chat/formatChatTimestamp.ts` (pure no-deps helper: HH:mm if today, "Yesterday" if yesterday, "MMM D" otherwise; comparisons use local midnight to respect device timezone); added `src/features/chat/screens/ChatRoomScreen.tsx` (placeholder returning null — story 15.5 replaces). Labels chat.list.{headerTitle,noMessagesYet,emptyTitle,emptyDescription,rowAccessibility} added to EN + UR with full parity. Tests: 10 formatChatTimestamp + 8 ChatListRow + 3 ChatListScreen = 21 new tests; 2501 → 2522 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. Non-obvious decisions: (a) ChatListRow uses a `View`-based layout (not Row/Column components) because the text column requires `flex:1` + `minWidth:0` — Row/Column components don't expose a style prop; (b) loading and empty states both show the same "No messages yet" fallback — hydration is fast (< 20 ms on mock store) so no flicker occurs; (c) ChatRoomScreen is a null-rendering placeholder to satisfy the route registration requirement without pulling in any unimplemented UI. Tracking issue #130 closed.
- 2026-08-27: Story 15.3 complete (commit 9e0765f, branch feat/phase-15-friend-req-chat-shell). ChatProvider + useChatHistory hook: added `src/state/chat/ChatProvider.tsx` exporting `ChatProvider` component, `useChatHistory(friendUserId)` hook, and `MEHVISH_USER_ID` constant derived from dummymehvish.json; provider holds a Map<friendUserId, messages[]> cache in a useRef so multiple useChatHistory consumers for the same friendUserId share one entry; hydration on first mount reads getHistory — if empty and Mehvish, seeds from chatMehvish.json `.messages` (not root) via saveHistory then re-reads; non-Mehvish starts empty; sendMessage trims input (empty/whitespace is no-op + console.warn), generates id via crypto.randomUUID() fallback, calls appendMessage then bumps in-memory version; updateMessage/deleteMessage delegate to storage helpers and refresh mirror; mounted ChatProvider in App.tsx between BookmarksProvider and NavigationContainer with TODO(mock-only) comment. Non-obvious decisions: (a) version counter (Map<friendUserId, number> in useState) used to signal re-renders when cacheRef (useRef) mutates — avoids re-rendering the full provider tree on every mutation; (b) AC (h) test uses a single React tree with two sibling consumer components rather than two independent renderHook wrappers (which would create separate provider instances). Tests: 11 new; 2490 → 2501 pass (3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. Tracking issue #129 closed.
- 2026-08-27: Story 14.2 complete (commit fc0531b, branch feat/phase-14-bookmarks). Wire Star button on MarriageLandingScreen → bookmark toggle: replaced phase-13 `t('landing.actionUnavailable')` snackbar on Star tap with real bookmark toggle via `useBookmarks()` — `addBookmark(currentDeck)` + `landing.bookmark.added` snackbar when not yet bookmarked; `removeBookmark(currentDeck.user_id)` + `landing.bookmark.removed` snackbar when already bookmarked. Deck index does NOT advance on Star tap. Added optional `isSuperLikeActive?: boolean` prop to `CollapsingActionBar` (defaults `false`); Star icon renders `fill={theme.colors.status.info}` when `true` to reflect bookmarked state. `MarriageLandingScreen` passes `isBookmarked(DECK_FIXTURES[currentDeckIndex]?.user_id ?? '')`. New labels `landing.bookmark.added` / `landing.bookmark.removed` added to EN + UR with full parity. `landing.actionUnavailable` key preserved in both label files. Tests: extended `MarriageLandingScreen.deck.test.tsx` — removed retired actionUnavailable Star assertion, added two new Star bookmark toggle tests (add + remove paths with mock assertions); added `jest.mock('@/state/bookmarks/BookmarksProvider')` to all three MarriageLandingScreen test files. 41 total tests in those files (all passing). tsc: zero errors. Tracking issue #123 closed.
