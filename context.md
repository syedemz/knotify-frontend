# knotify-frontend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Not yet started. Awaiting `architecture.md` and `/create-plan`.

## Active blockers
None. Phase 1 gates §17.22 and §17.26 both resolved on 2026-07-19.

## Before shipping

Local auth mock mode is active while the AWS backend is unspun (to save cost during UI walkthrough). Before the first real production build, delete the mock scaffolding in this order:

1. **`src/config/env.ts`** — delete the `isMockAuth` field from the exported `env` object (currently keyed off the sentinel `cognito.userPoolId === 'eu-central-1_dummydummy'`).
2. **`src/services/auth/cognitoClient.ts`** — delete every `if (env.isMockAuth) { ... }` branch (currently in `ensureConfigured`, `signUp`, `confirmSignUp`, `signIn`, `signOut`, `refreshSession`) and delete the `makeMockTokens` helper.
3. **`src/config/backendConfig.dev.json`** — replace the dummy sentinel values (`eu-central-1_dummydummy`, `dummydummydummydummydummy1`, `dummy.example.invalid`, `dummy.appsync-api...`, `dummy.cloudfront.net`) with the real values emitted by the backend Terraform outputs. `env.ts` throws at import time on any `<FILL_*>` placeholder, so partial fills are safe.

After all three are done, `grep -r isMockAuth src/` should return zero hits and `grep -r dummydummy src/config/` should return zero hits. Add a CI grep gate at that time if you want a hard stop.

## Critical design decisions
None yet.

## Recent changes
- 2026-07-25: Dev workflow shifted from Expo Go to Android **development build** (PR #91 merged). `npm run android`/`ios` now run `expo run:android`/`ios` (was `expo start --android`/`ios`). Trigger: Page 14's `expo-notifications` push-token call crashes Expo Go in SDK 53+ ("remote notifications functionality removed"). CNG kept — `android/`, `ios/` stay gitignored; `app.json` authoritative. Requires Temurin JDK 17 at `C:/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot` (Android Studio's bundled JBR is Java 11; Gradle 9.3.1 rejects it). Build+install via `npx expo run:android` onto `Pixel_6_Pro_API_34` (`emulator-5554`); Metro on `localhost:8081` hot-reloads JS/TS edits (no rebuild); `npx expo prebuild` only needed after `app.json` plugin/native changes. Reset onboarding state on emulator with `adb shell pm clear com.knotify.app` (wipes secure-store without reinstalling). Do NOT test via Expo Go / QR code / `expo start`.
- 2026-07-25: Onboarding draft store refactored to Context/Provider (PR #92 merged). Root cause of Page 13 empty-render bug: `useOnboardingDraft` was a plain hook, so each screen owned its own state+ref instance — Page 12 wrote `education_level` to its instance and auto-advanced before the 200ms debounced SecureStore write landed, so Page 13 mounted with a fresh empty draft and `visibleFields(null)` returned no fields. Fix: `src/features/onboarding/hooks/useOnboardingDraft.tsx` (renamed from `.ts`) now exports `OnboardingDraftProvider` (owns single state instance via internal `useOnboardingDraftState`) and consumer `useOnboardingDraft` (throws if not inside provider). `OnboardingStack` split into outer wrapper (mounts provider) + inner navigator (consumes draft for `useCheckpointResume`+`isLoading` gate). All 15 test `jest.mock` blocks added `OnboardingDraftProvider: ({ children }) => children` passthrough; `useOnboardingDraft.test.ts` wraps `renderHook` with `wrapper: OnboardingDraftProvider`. 1285/1285 tests pass. Verified on emulator: after `adb shell pm clear`, walked Page 1 → 12 → 13, credential fields render.
- 2026-07-25: Story 6.1 complete (branch feat/phase-6-residence-b5). Page15CountryScreen: searchable country picker with flag (via `react-native-country-flag`), name, and dial code per row; tapping a row atomically writes `current_residence_country` and `resident_country_code` in one `update({...})` call, then auto-advances to `Page16ResidenceCityScreen`. `CountryPicker` added to shared catalog at `src/components/CountryPicker.tsx` (uses ScrollView+map for RNTL test-compatibility; 200 entries render fine on device). Search is case-insensitive prefix match on name only; no debounce. No default pre-selection. Labels `onboarding.country.title` and `onboarding.country.searchPlaceholder` added with Urdu parity. `react-native-country-flag` installed (CJS-only, no SVG — no extra `moduleNameMapper` needed). OnboardingStack wired (placeholder replaced); OnboardingStack test updated to use real title. 31 new tests (20 CountryPicker component + 11 Page15 wiring); 1316/1316 pass; coverage 87.38/84.26/87.64/88.37 (all above threshold). tsc: zero errors; eslint: zero errors. Non-obvious decisions: (a) ScrollView+map instead of FlatList — RNTL's FlatList virtual rendering skips items beyond ~10 rendered by default, making testID lookups for mid-list entries (e.g. Pakistan) fail; ScrollView renders all items synchronously; (b) `react-native-country-flag` uses `react-native`'s own `Image` with a CDN PNG URL — no native/SVG modules involved, so Jest runs without any moduleNameMapper addition.
- 2026-07-25: Story 6.2 complete (commit 3da94fa, branch feat/phase-6-residence-b5). Page16ResidenceCityScreen: single TextInput (maxLength=41, mirroring phase-11 over-limit pattern), touched-flag inline errors (tooLong / invalidCharacters), Continue gates on `isValidCity()`. On Continue, writes `current_residence_city` to draft, calls `advance(17)`, navigates to `Page17FamilyResidenceScreen`. `isValidCity` and `MAX_CITY_NAME_LENGTH = 40` added to validationHelper.ts — separate from `isValidName` (which caps at 35) per AC; same character class (letters, spaces, hyphens, apostrophes; no leading/trailing whitespace). Labels under `onboarding.residenceCity.*` (title, label, placeholder, errors.tooShort/tooLong/invalidCharacters) added to both EN and UR with full parity. OnboardingStack placeholder for page 16 replaced with real screen. 44 new tests (23 isValidCity unit tests covering boundary lengths 0/1/40/41, whitespace rejection, allowed chars, rejected chars; 21 screen wiring tests covering mount, disabled/enabled transition, error display, draft write, advance, back nav); 1316 → 1360 pass; coverage 87.57/84.34/87.81/88.58 (all above threshold). tsc: zero errors; eslint: zero errors (56 pre-existing warnings only).
- 2026-07-25: Story 6.3 complete (commit 8dfefcb, branch feat/phase-6-residence-b5). Page17FamilyResidenceScreen: 22-district radio picker (`options.kashmirDistricts`) plus multiline TextInput for family home address. Mount-write semantics: on first mount, if `draft.fields.district` is null/undefined, immediately writes `"Srinagar"` to the draft so the draft and the visually-selected row stay in sync; guard prevents re-write on back-nav (read the draft value inside the effect, check null/undefined before calling update). `isValidFamilyResidenceAddress` and `MAX_FAMILY_RESIDENCE_ADDRESS_LENGTH = 70` added to validationHelper.ts — no character-class restriction (mirrors `isValidOfficeAddress`), trims before length check. Continue gates on district non-null AND valid address; on tap writes `district` + `family_residence_address` and navigates to `Page18ParentsScreen`. Non-obvious decision: the PRD uses shorthand `kashmir_district` throughout but the actual `UserProfile` / `DraftFields` field name is `district` — all update/getDraft calls use `district`. Labels under `onboarding.kashmirDistrict.title` and `onboarding.familyResidenceAddress.{label,placeholder,errors.tooShort/tooLong}` added to EN + UR with parity. OnboardingStack placeholder for page 17 replaced with real screen. 39 new tests (11 isValidFamilyResidenceAddress unit tests + 28 screen wiring tests); 1360 → 1399 pass; coverage 87.76/84.46/88.1/88.8 (all above threshold). tsc: zero errors; eslint: zero errors (59 pre-existing warnings only). PR #96 opened (feat/phase-6-residence-b5 → development).
- 2026-07-25: Phase 6 (Residence — pages 15-17) complete. All 3 stories (6.1-6.3) shipped: Page 15 country picker with flag + dual-field atomic write (`CountryPicker` catalog component, `react-native-country-flag`), Page 16 city name with `isValidCity` (40-char, letter/space/hyphen/apostrophe), Page 17 Kashmir district radio picker with Srinagar default-preselect + family residence address (`isValidFamilyResidenceAddress`, 70-char, no char-class rule). Tracking issues #93-#95 closed. PR #96 open (feat/phase-6-residence-b5 → development). Non-obvious decisions: (a) UserProfile field for Kashmir district is `district`, not `kashmir_district` as the PRD shorthand suggests; (b) ScrollView+map used in CountryPicker instead of FlatList for RNTL test-compatibility (FlatList virtual rendering skips items beyond ~10); (c) mount-write guard uses empty dep array + reads draft inside the effect to correctly detect first-visit vs back-nav; (d) `MAX_FAMILY_RESIDENCE_ADDRESS_LENGTH` validator trims before length check so leading/trailing whitespace does not count as content. 1360 → 1399 tests; final coverage 87.76/84.46/88.1/88.8.
- 2026-07-26: Story 7.1 complete (commit dca7e9b, branch feat/phase-7-family-b6). Page18ParentsScreen: 4 TextInputs (fathers_name/mothers_name via isValidParentName, max 40; fathers_job/mothers_job via isValidParentJob, max 40 with ampersand/period/digit allowed) + 2 yesNo pick-ones (father_retired/mother_retired, written as "YES"/"NO"). Single atomic update({...}) on Continue; back-nav re-hydrates all 6 fields from draft.fields. Labels under onboarding.parents.* in EN+UR. OnboardingStack placeholder → real screen. noUncheckedIndexedAccess in tsc required getByLabelTextAt() null-guard helper in test (same issue as Page17's backButtons[0] guard). 72 new tests; 1471 total pass; coverage 88.15/84.94/87.86/89.17. tsc: zero errors; eslint: zero errors. Tracking issue #31 closed. PR #97 open (feat/phase-7-family-b6 → development).
- 2026-07-26: Story 7.2 complete (commit 8ce7af4, branch feat/phase-7-family-b6). SiblingDraft extended with gender/profession fields; schemaVersion 1→2 with DISCARD policy in useOnboardingDraft (pre-launch, no real user data). isValidProfession + MAX_SIBLING_PROFESSION_LENGTH=35 added to validationHelper.ts. SiblingForm feature-local component (5 fields: name/gender/sibling_age/marital_status/profession) with onChange/onValidityChange callbacks. Page19SiblingsScreen: three-state machine (Initial/Filling/Complete) — count=0 writes draft on Continue tap only; count>=1 transitions to Filling; Cancel from Filling returns to Initial without touching draft.siblings; back-nav re-hydration reads draft.currentPage+draft.siblings. 15 pre-existing test fixtures updated for schemaVersion bump. 57 new tests; 1528 total pass; coverage 88.73/85.43/88.6/89.77. tsc: zero errors; eslint: zero errors. Tracking issue #32 closed.
- 2026-07-26: Phase 7 (Family — pages 18-19) complete. Both stories (7.1-7.2) shipped: Page 18 parents (6 fields, 2 validators), Page 19 siblings dynamic list (SiblingForm sub-form, three-state machine, schemaVersion 2 DISCARD policy). Tracking issues #31-#32 closed. PR #97 open (feat/phase-7-family-b6 → development). Non-obvious decisions: (a) schemaVersion bump from 1→2 required updating 15 test fixtures; (b) SiblingForm uses accessibilityLabel pattern "FieldLabel index" (e.g. "Male 0") to distinguish per-card radio rows under noUncheckedIndexedAccess; (c) Page20 route name is Page20MarriageTimelineScreen (not MarriageTimeScreen as AC shorthand suggested); (d) pageMap route is Page15ResidenceCountryScreen but screen file is Page15CountryScreen — OnboardingStack aliases handle this. 1471→1528 tests; final coverage 88.73/85.43/88.6/89.77.
- 2026-07-26: Story 8.1 complete (branch feat/phase-8-lifestyle-b7). Page20MarriageTimelineScreen: two stacked PillPickOneGroup sections — first (primary, "know someone for") is client-local useState only, hardcoded 4 options, never persisted; second (secondary, "married within") reads options.marriageTime and writes draft.fields.marriage_time. Auto-advance to Page21OwnReligiousLevelScreen fires in tap handlers only (not useEffect) when both selections are non-null. On re-visit, secondary rehydrates from draft.fields.marriage_time; primary always starts empty. `PillPickOneGroup` new catalog component added (`src/components/PillPickOneGroup.tsx`) — wrapping Row of PillButtons with subheader Text; exports from index.ts; fully tested. `marriageTime.json` updated to 7-option list ["1-2 months","3-4 months","4-12 months","1-2 years","3-4 years","4+ years","Agree together"] (retires previous "Within N months" format). Labels `onboarding.marriageTimeline.{title,primary.label,secondary.label}` added to EN+UR with full parity. OnboardingStack placeholder for page 20 replaced with real screen. 36 new tests (13 PillPickOneGroup component + 23 screen wiring); 1528 → 1564 pass. tsc: zero errors; eslint: zero errors. Tracking issue #98 closed. Non-obvious decisions: (a) auto-advance is wired inside tap handlers (not useEffect) to prevent back-nav rubber-banding; (b) the draft write for marriage_time is deferred until the completing tap so partial selections leave the draft unchanged; (c) on re-visit when marriage_time is already in draft, tapping primary triggers advance immediately using the stored draft value.
- 2026-07-26: Story 8.2 complete (branch feat/phase-8-lifestyle-b7). Page21OwnReligiousLevelScreen: vertical radio-picker over options.religiousLevel (4 items), writes religious_level on tap, auto-advances to Page22PartnersReligiousLevelScreen from tap handler only (not useEffect). On re-visit, initializedRef guard rehydrates selected row from draft without re-firing advance. Labels onboarding.ownReligiousLevel.title added to EN+UR with full parity. OnboardingStack placeholder for page 21 replaced with real screen. 16 new tests; 1564 → 1580 pass. tsc: zero errors. Tracking issue #99 closed. Non-obvious decision: ListRowSelectable exposes accessibilityState.checked (not .selected) because it uses accessibilityRole="radio" — test assertions use .checked accordingly.
