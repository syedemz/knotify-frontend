# knotify-frontend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Not yet started. Awaiting `architecture.md` and `/create-plan`.

## Active blockers
None. Phase 1 gates §17.22 and §17.26 both resolved on 2026-07-19.

## Before shipping

Local auth mock mode is active while the AWS backend is unspun (to save cost during UI walkthrough). Before the first real production build, delete the mock scaffolding in this order:

1. **`src/config/env.ts`** — delete the `isMockAuth` field from the exported `env` object (currently keyed off the sentinel `cognito.userPoolId === 'eu-central-1_dummydummy'`).
2. **`src/services/auth/cognitoClient.ts`** — delete every `if (env.isMockAuth) { ... }` branch (currently in `ensureConfigured`, `signUp`, `confirmSignUp`, `signIn`, `signOut`, `refreshSession`) and delete the `makeMockTokens` helper.
3. **`src/config/backendConfig.dev.json`** — replace the dummy sentinel values (`eu-central-1_dummydummy`, `dummydummydummydummydummy1`, `dummy.example.invalid`, `dummy.appsync-api...`, `dummy.cloudfront.net`) with the real values emitted by the backend Terraform outputs. `env.ts` throws at import time on any `<FILL_*>` placeholder, so partial fills are safe.

After all three are done, `grep -r isMockAuth src/` should return zero hits and `grep -r dummydummy src/config/` should return zero hits. Add a CI grep gate at that time if you want a hard stop.

### Mock-only registration + profile pipeline (added phase 11, 2026-08-03)

Registration completion and the phase-12 profile pipeline are intentionally mock-only until the backend (Cognito + AWS API + DB) is stood up. Every temporary surface added by phases 11–12 is grep-able via `TODO(mock-only)`. Before the first real production build, remove them in this order:

1. **Wire up `custom:profile_complete` JWT claim decode in `src/state/auth/AuthProvider.tsx`.** Resolve the `TODO(phase-2)` in the `profileComplete` `useMemo` (lines ~122-129) — decode the ID token, read the `custom:profile_complete` claim, return the boolean. This is what actually unlocks AppTabs post-onboarding once real Cognito is in play.
2. **Verify Cognito refresh returns a fresh ID token with the updated claim.** `AuthProvider.refresh()` calls `cognitoClient.refreshSession()`. If Amplify caches the ID token instead of re-fetching from Cognito after the backend flips `profile_complete_verified`, the claim will stay stale — in which case we need a manual `/oauth2/token` call or a sign-out/sign-in round-trip after the final PATCH. Verify with a real Cognito user pool before removing the mock-only bypass.
3. **Delete `src/state/onboardingCompletion/`** (the `OnboardingCompletionProvider` and its context) and remove its mount from `App.tsx`.
4. **Delete the additive `dummyOnboardingComplete` check in `src/navigation/RootNavigator.tsx`** — restore to a pure `profileComplete`-driven gate.
5. **Delete the mock dispatch branch in `src/services/api/httpClient.ts`** (the `if (env.isMock) { … mockRequest … }` at the top of `request()`) and **delete `src/services/api/mocks/mockRequest.ts`**. Once removed, real HTTP client calls the real backend for every path. Note: this replaces the earlier MSW-based mock (see 2026-08-04 change) — MSW was retired on Hermes because `msw/native` depends on browser globals that would require fragile polyfills.
6. **Delete the secure-store snapshot logic in `Page31FaceCaptureScreen`** — the `dummy.profile` write and the `dummy.onboarding.complete` flag write on mock-submit success.
7. **Delete the secure-store read logic in the mock GET `/profile/me` handler** and the merge logic in the mock PATCH `/profile/me` handler.
8. **One-shot migration on next launch:** wipe secure-store keys `dummy.profile` and `dummy.onboarding.complete` so no stale local data lingers.
9. **Verify YES/NO backend casing.** Fields `father_retired`, `mother_retired` (TEXT columns per phase 7) and `has_children`, `move_abroad` (BOOL columns per phase 8, already coerced) are written as literal `"YES"` / `"NO"` (uppercase) in the draft. Confirm the real backend accepts this exact casing for the two TEXT columns, or update `src/config/options/yesNo.json` (single source of truth) and re-run affected screens.

After all of the above: `grep -r 'TODO(mock-only)' src/` should return zero hits.

## Follow-up tickets

- **Real face-in-oval bounds check on Page 31.** The frame processor in `Page31FaceCaptureScreen.tsx` currently passes `faces.length > 0` as `faceInsideOval` — i.e. "any face detected anywhere in the frame", not "face bounds intersect the oval". Auto-capture threshold is bumped to 150 frames (~5s @ 30fps) as a stop-gap so the manual shutter button remains the practical capture path. Proper fix: read face bounds from `detectFaces()`, translate camera-sensor coordinates → screen coordinates (accounting for preview scaling, letterboxing, and front-camera mirroring), and gate `onFrame(true)` on real intersection with the `FaceOvalOverlay`'s bounds. Verify on ≥2 device sizes before shipping. Revert `CONSECUTIVE_FRAMES_REQUIRED` back to 15 once implemented.

## Critical design decisions
None yet.

## Recent changes
- 2026-07-26: Story 8.3 complete (branch feat/phase-8-lifestyle-b7). Page22PartnersReligiousLevelScreen: identical layout to page 21 — vertical radio-picker over options.religiousLevel, writes partners_religious_level on tap, auto-advances to Page23MaritalStatusScreen from tap handler only (not useEffect). On re-visit, initializedRef guard rehydrates selected row from draft.fields.partners_religious_level without re-firing advance. Labels onboarding.partnersReligiousLevel.title added to EN+UR with full parity. OnboardingStack placeholder for page 22 replaced with real screen. 16 new tests; 1580 → 1596 pass. tsc: zero errors; eslint: zero errors. Tracking issue #100 closed.
- 2026-07-26: Story 8.4 complete (branch feat/phase-8-lifestyle-b7). Page23MaritalStatusScreen: reactive two-stage screen — Never Married tap atomically writes { marital_status, has_children: false } and advances; Divorced/Widowed tap reveals children sub-question via local state only (no draft write); Yes/No tap writes both fields atomically (has_children coerced from 'YES'/'NO' → boolean) and advances. Reset rule enforced: Never Married handler always writes has_children=false unconditionally. Re-visit reconstructs local UI state from draft.fields.marital_status + draft.fields.has_children; auto-advance never fires on mount (no useEffect advance). Labels onboarding.maritalStatus.{title,hasChildren.label} added to EN+UR with full parity. OnboardingStack placeholder for page 23 replaced with real screen. 47 new tests; 1596 → 1643 pass; coverage 88.96/85.43/88.53/90.2. tsc: zero errors; eslint: zero errors. Tracking issue #101 closed. Non-obvious decisions: (a) pendingBranchStatus is local state, never written to draft, so partial Divorced selection survives a back-nav within the session but is discarded on full re-visit; (b) has_children: false written for Never Married is a boolean, not a string — tsc enforces this via DraftFields typing; (c) re-visit with has_children=false correctly highlights NO (explicit false vs null distinction preserved).
- 2026-07-26: Story 8.5 complete (branch feat/phase-8-lifestyle-b7). Page24MoveAbroadScreen: single YES/NO radio picker — first visit initialises selected='YES' as a visual-only default (no draft write on mount); tap coerces to boolean (move_abroad: tapped==='YES'), writes draft, calls advance(25), navigates to Page25Preferences1Screen; re-visit with move_abroad=true lights YES, re-visit with move_abroad=false lights NO, auto-advance never fires from mount. Labels onboarding.moveAbroad.title added to EN+UR with full parity. OnboardingStack placeholder for page 24 replaced with real screen. 26 new tests; 1643 → 1669 pass. tsc: zero errors. Tracking issue #102 closed. Non-obvious decision: null/false distinction in rehydration is critical — null (no prior answer) maps to the YES pre-select without writing draft; false (explicit NO answer) maps to NO highlight. Both avoid auto-advance.
- 2026-07-26: Phase 8 (Lifestyle + marital status — pages 20-24) complete. All 5 stories (8.1-8.5) shipped: Page 20 marriage timeline (dual pick-one, first client-local, second updates 7-option marriageTime catalog; new PillPickOneGroup catalog component), Page 21 own religious level (vertical radio), Page 22 partner's religious level (identical layout to page 21), Page 23 marital status (reactive Divorced/Widowed → children Yes/No, atomic writes, symmetric reset rule for Never Married), Page 24 move abroad (visual-only YES pre-select, boolean coercion, null/false rehydration distinction). Tracking issues #98-#102 closed. PR #103 open (feat/phase-8-lifestyle-b7 → development). Non-obvious phase-wide patterns: (a) `has_children` and `move_abroad` are `boolean | null` in UserProfile, so yesNo taps coerce `tapped === 'YES'` at write time (never write raw strings — tsc rejects); (b) auto-advance always wired inside tap handlers, never useEffect on draft state — prevents back-nav rubber-banding; (c) initializedRef guard pattern reused across pages 21/22/23/24 to safely rehydrate UI state without triggering advance on mount; (d) preselect is visual-only (component useState init), never a mount-time draft write, so back-nav distinguishes "user explicitly answered" from "default preselect shown". 1528 → 1669 tests (+141); final coverage 88.96/85.43/88.53/90.2 (all above threshold).
- 2026-07-26: Story 9.1 complete (branch feat/phase-9-preferences-b8). Page25PersonalityTraitsScreen: 57-trait pill multi-select (from new src/config/options/preferences1.json) with 5-trait selection cap (6th tap shows Snackbar toast), Continue button enabled at N>=1 ("Select (N)" label), Skip button (ghost variant) advances without writing, writes preferences: { personalityTraits: [...] } to draft.fields.preferences on Continue tap only; auto-advances to Page27RelationScreen (skipping unused page 26 slot). On re-visit rehydrates pre-selected pills from draft.fields.preferences?.personalityTraits via initializedRef guard — no auto-advance on mount. Labels onboarding.personalityTraits.{title,subtitle,continueLabel,skip,capExceededToast,options.*} added to EN+UR with full parity (41 non-MBTI traits translated, 16 MBTI acronyms same in both languages). OnboardingStack placeholder for page 25 replaced with real screen; router updates 25 → 27 directly (no page 26 stop). options.test.ts updated to include preferences1 in ALL_KEYS. 40 new tests (36 Page25 screen wiring + 4 options registry); 1669 → 1709 pass; coverage 89.1/85.45/88.37/90.39 (all above threshold). tsc: zero errors; eslint: zero errors. Tracking issue #104 closed. Non-obvious decisions: (a) Text component has no style prop — subtitle margin handled via View wrapper; (b) Set<string> for selected state provides O(1) toggle/has semantics; (c) Skip uses Button variant="ghost" directly (not WizardFooter.onBack) because onBack renders the "Back" label, not "Skip".
- 2026-07-26: Story 9.2 complete (branch feat/phase-9-preferences-b8). Page27RelationScreen: vertical radio-picker over options.relation (6 items: Myself/Son/Daughter/Sibling/Friend/Ward), writes relation on tap, auto-advances to Page28PhotosScreen from tap handler only (not useEffect). On re-visit, initializedRef guard rehydrates selected row from draft.fields.relation without re-firing advance. Labels onboarding.relation.{title,options.*} added to EN+UR with full parity. OnboardingStack placeholder for page 27 replaced with real screen. 18 new tests; 1709 → 1727 pass. tsc: zero errors; eslint: zero errors (7 pre-existing warnings unchanged). Tracking issue #105 closed.
- 2026-07-26: Phase 9 (Preferences + relation — pages 25-27) complete. Both stories (9.1-9.2) shipped: Page 25 personality traits multi-select (57-trait pill grid, 5-trait cap with Snackbar, Continue+Skip, writes preferences: { personalityTraits: [...] } into existing JSONB field — no schema bump, router jumps 25 → 27), Page 27 relation picker (vertical ListRowSelectable, tap-writes-and-advances). Original story 9.2 (interests multi-select) dropped from scope per user 2026-07-26 decision; page 26 slot unused. Tracking issues #104-#105 closed. PR #106 open (feat/phase-9-preferences-b8 → development). Non-obvious phase-wide decisions: (a) preferences JSONB field storage shape `{ personalityTraits: string[] }` uses the existing UserProfileWritable.preferences slot without any draft schema changes — resolves the previously deferred §17.20 [Open]; (b) MBTI acronyms deliberately untranslated in Urdu because they are international technical codes; (c) Skip button uses Button variant="ghost" directly (not WizardFooter.onBack, which renders the "Back" label); (d) selection cap enforced via Set<string> local state with Snackbar toast on 6th tap; (e) initializedRef guard reused on both stories to prevent mount-time auto-advance on re-visit. 1669 → 1727 tests (+58); final coverage 89.1/85.45/88.37/90.39 (all above threshold).
- 2026-07-26: fix/page-25-personality-pill-layout — post-merge UI polish on Page25PersonalityTraitsScreen. Four fixes applied: (1) pill layout changed from fixed flexBasis:'47%' two-column grid to natural flex-wrap with gap:sm so pills size to content width; (2) emoji icons added to all 57 traits via new src/config/options/preferences1Icons.json (PillButton gains `icon?: string` + `borderVariant?: 'default'|'subtle'` props; backwards-compatible, PillPickOneGroup on Page 20 unaffected); (3) unselected pill borders softened to StyleSheet.hairlineWidth + border.default colour (borderVariant='subtle') matching Muzz reference screenshots; (4) Continue always enabled — at N=0 label reads "Continue" (new continueLabelEmpty label) and no draft write occurs; Skip button removed entirely. Labels: `skip` key removed from EN+UR, `continueLabelEmpty` added. Tests: 36 → 35 (removed disabled-at-0 + all Skip assertions; added 3 AC(v) "0-selections → no write, advance" tests). 1727 → 1726 total pass; tsc: zero errors; eslint: zero new warnings. PR opened fix/page-25-personality-pill-layout → development.
- 2026-08-03: Story 10.1 complete (bd8445b, branch feat/phase-10-photos-phone). Page28PhotosScreen: 6-tile flex-wrap photo grid; expo-image-picker (native module — full APK reinstall required on device 000323572000090 after merge); requestMediaLibraryPermission() added to permissions service; setPhotoPreviewUris() added to useOnboardingDraft hook; PhotoTile feature-local component (empty/filled/denied states, remove badge, body-press-to-replace); bg.overlay token added to theme; labels onboarding.photos.* added EN+UR; Page29PhoneScreen stub added; OnboardingStack wired to real Page28 + stub Page29. 37 new tests (16 PhotoTile + 21 Page28); 1726→1763 pass. tsc: zero errors. Known limitation: Android content:// URIs may not survive cold restart until backend photo upload (§17.14) ships.
- 2026-08-03: Story 10.2 complete (19f3e12, branch feat/phase-10-photos-phone). Page29PhoneScreen: real implementation replacing stub — dial-code chip (defaults from resident_country_code lookup, fallback +91), TextInput number-pad maxLength 15, CountryPicker modal override (same component as page 15), Continue gated by isValidPhone(), writes E.164 via canonicalizePhone() to draft.phone_number, advances to Page30FaceVerifyIntroScreen. libphonenumber-js installed (JS-only, no prebuild). Draft schemaVersion bumped 2→3 (DISCARD policy: v2 drafts wiped on load — QA concern #8). Note: PRD claimed schema was already at v3 from phase 9, but observed state was v2 (phase 9's personalityTraits change never bumped it); the bump-by-one from actual state is correct and DISCARD semantics still hold. isValidPhone + canonicalizePhone added to validationHelper.ts (// ── phone ── section). Labels onboarding.phone.* added EN+UR (7 keys, full parity). ~22 test fixtures updated to schemaVersion 3 + phone_number: null. 37 new tests (20 validationHelper + 16 Page29 screen wiring + 1 draftSchema); 1763→1800 pass. tsc: zero errors; eslint: zero errors. Tracking issue #42 closed.
- 2026-08-03: Phase 10 (Photos + phone — pages 28-29) complete. Both stories (10.1-10.2) shipped: Page 28 photos (6-tile grid + expo-image-picker native module + media-library permission + PhotoTile component with remove/replace UX) and Page 29 phone (libphonenumber-js validation + dial-code chip + CountryPicker override + E.164 canonicalization + phone_number draft field). Tracking issues #41-#42 closed. PR #108 open (feat/phase-10-photos-phone → development). Non-obvious phase-wide decisions: (a) expo-image-picker is a native module — merging PR #108 requires `npx expo run:android` full APK reinstall on device 000323572000090 (READ_MEDIA_IMAGES permission activates on first install of new APK); (b) libphonenumber-js is JS-only, no prebuild needed — a Metro reload picks up story 10.2 alone; (c) default dial code is +91 India (per user 2026-08-03 targeting decision, not +92 Pakistan as originally suggested); (d) dial-code chip reuses the same CountryPicker component from page 15 rather than duplicating; (e) phone_number stored E.164 canonical (`+919812345678`), not split into dial/national; (f) Android content:// URIs for photos may not survive cold restart until backend upload (§17.14) — known limitation, not a bug for v1. 1727 → 1800 tests (+73); coverage above threshold.
- 2026-08-03: Story 11.1 complete (7c14961, branch feat/phase-11-face-verify-b10). Page30FaceIntroScreen: face verification intro + camera permission gate using react-native-vision-camera@~4.7.3 + vision-camera-face-detector@~0.1.8 (both native modules — full APK reinstall required on device 000323572000090 after merge); requestCameraPermission() added to permissions service with explicit mapVisionCameraStatus helper mapping 'granted'→'granted', 'denied'→'denied', 'not-determined'→'undetermined', 'restricted'→'denied'; __mocks__/ added for both vision-camera packages; Page30 renders explainer + "Verify photo" button, escalates denial state from none→firstDeny→permanent (first deny shows Retry only; permanent deny adds "Open Settings" Linking.openSettings deep link + Snackbar toast reusing Page25 pattern); labels onboarding.faceIntro.* added EN+UR (8 keys, full parity); OnboardingStack Page30 placeholder replaced with real screen. 26 new tests (10 cameraPermission + 16 Page30); 1800→1826 pass. tsc: zero errors; eslint: zero errors. Tracking issue #43 closed.
- 2026-08-03: Story 11.2 complete (branch feat/phase-11-face-verify-b10). Page31FaceCaptureScreen: full-screen camera preview (react-native-vision-camera Camera + useFrameProcessor) with FaceOvalOverlay (border-radius transparent oval, pointerEvents none); useAutoCaptureController pure state machine (15 consecutive true frames → shouldCapture, useRef counter, no re-render per frame); manual shutter button always visible; 5-step mock-only submit sequence (buildPatchBody → PATCH /profile/me via MSW → SecureStore dummy.profile snapshot → markComplete → clear); 409 username regeneration+retry; 500/network error surface with retry; state-driven navigation (markComplete() triggers RootNavigator re-render, NO navigation.navigate call). OnboardingCompletionProvider: reads/writes dummy.onboarding.complete from expo-secure-store, loading guard prevents first-mount race, resilience on read errors, runtime sanity warn on complete===true+unauthenticated. buildPatchBody pure function: explicit allowlist, isRetry strips immutable fields, serializeSiblings camelCase→snake_case. RootNavigator: !profileComplete && !mockOnboardingComplete gate + loading guard extended to include onboardingCompletion.loading. App.tsx tree updated with OnboardingCompletionProvider wrapping NavigationContainer. OnboardingDraft schemaVersion 3→4 + faceSelfieUri: string|null; ~25 fixture files updated. Button component gains testID prop forwarded to Pressable. react-native-vision-camera mock gains useFrameProcessor. Labels onboarding.faceCapture.* added EN+UR (8 keys, full parity). 84 new tests (21 Page31 + 12 useAutoCaptureController + 11 buildPatchBody + 18 OnboardingCompletionProvider + 2 auth-gate + 20 existing fixture updates); 1826→1910 pass. tsc: zero errors; eslint: zero errors. Tracking issue #44 closed.
- 2026-08-03: Phase 11 (Face verification + final PATCH — pages 30-31) complete. Both stories (11.1-11.2) shipped: Page 30 face intro (vision-camera permission gate with escalating denial UX + OS-settings deep link + Snackbar toast) and Page 31 face capture (full-screen camera + FaceOvalOverlay + pure state machine auto-capture + always-visible manual shutter + 5-step mock-only submit + OnboardingCompletionProvider + additive RootNavigator gate). Tracking issues #43-#44 closed. PR #109 open (feat/phase-11-face-verify-b10 → development). **This phase intentionally does NOT integrate the real backend** — per user decision 2026-08-03 (concern #3 in QA/explanations.txt), the AuthProvider stub and `custom:profile_complete` JWT claim decode remain untouched; registration completion is bypassed client-side via `dummy.onboarding.complete` in expo-secure-store and an additive `mockOnboardingComplete` check in RootNavigator. The assembled PATCH body is snapshotted to `dummy.profile` so phase 12's MyProfileScreen can read real user-entered data instead of a fixture. Every mock-only surface marked `// TODO(mock-only): remove when real backend + JWT claim decode ship` — teardown is a 9-item checklist in `context.md → Before shipping → Mock-only registration + profile pipeline`. Non-obvious phase-wide decisions: (a) `react-native-vision-camera` + `vision-camera-face-detector` chosen over expo-camera (§17.24 [Open] closed) — both native modules, merging PR #109 requires `npx expo run:android` full APK reinstall on device 000323572000090 to activate CAMERA permission; (b) `useAutoCaptureController` state machine uses `useRef` counter, not `useState`, to avoid per-frame re-renders during face detection; (c) navigation swap on submit success is state-driven (`markComplete()` flips context → RootNavigator re-renders → AppTabs), NOT imperative — screen never calls `navigation.navigate('AppTabs')`; (d) `OnboardingCompletionProvider` mounted as a descendant of AuthProvider (required for `useAuth()` sanity warning); (e) runtime sanity warn only fires on `status === 'unauthenticated'`, not `'loading'`, to avoid spurious warnings on every cold launch for completed users; (f) `markComplete()` owns both the secure-store write AND the in-memory flip in one method to prevent inconsistent state; (g) OnboardingDraft schemaVersion bumped 3→4 (DISCARD policy) for the `faceSelfieUri` field, ~25 fixtures updated in one mechanical pass; (h) `buildPatchBody` retry test iterates all `isImmutable()` keys — future additions to the helper are covered without editing the test. 1800 → 1910 tests (+110); coverage above threshold.
- 2026-08-04: Story 12.1 complete (branch feat/phase-12-landing-profile). Dummy JSON fixtures + type declarations: added `assets/dummyfemale.json` (Aisha Khan, 27, Berlin DE, Product Designer, 2 siblings, 2 photos, 5 personalityTraits, gold tier) and `assets/dummyprofile.json` (Adnan Malik, 29, Mannheim DE, Software Engineer, 0 siblings, 1 photo, 1 personalityTrait "Night owl", __mutability_notes block). Added `src/types/DummySibling.ts`, `src/types/DummyFemaleProfile.ts`, `src/types/DummyOwnProfile.ts` — all extend `UserProfile` via `Omit<UserProfile, 'preferences'>` (necessary because the narrowed `preferences` type adds `| undefined` which conflicts with interface-extends assignability). Added `resolveJsonModule: true` to `tsconfig.json`. 32 new tests in `__tests__/assets/dummyFixtures.test.ts`; 1902 → 1934 pass. tsc: zero errors. Tracking issue #112 closed.
- 2026-08-04: Story 12.3 complete (branch feat/phase-12-landing-profile). Reusable Share primitive: added `src/features/profile/buildShareMessage.ts` (pure helper, returns `{ message: "Check out <first_name> on Knotify: knotify://profile/<user_id>", url }`, falls back to user_id when first_name is null); added `src/features/profile/components/ShareProfileButton.tsx` (three variants: icon-only/pill/row-link, delegates to `Share.share({ message, url })`, rejection swallowed with `.catch(() => {})`); added `share.*` keys (`share.action`, `share.messagePrefix`, `share.actionAccessibility`) to both `labels.en.json` and `labels.ur.json` with full parity. 12 new tests in `__tests__/features/profile/buildShareMessage.test.ts` (7) and `__tests__/features/profile/ShareProfileButton.test.tsx` (5 — icon-only a+b, pill a+b, row-link a+b, plus custom testID forwarding); 1934 → 1946 pass. tsc: zero errors. Non-obvious decision: `Share` is imported from `react-native` directly (`import { Share } from 'react-native'`) — no wrapper needed since `jest-expo` preset provides the standard react-native mock in the test environment; `jest.spyOn(Share, 'share')` resolves cleanly. Tracking issue #114 closed.
- 2026-08-04: Story 12.2 complete (branch feat/phase-12-landing-profile). Profile-section catalog + hide-if-empty guards: added `src/types/DummyOverlay.ts` (shared `DummyOverlay` interface + `ProfileViewer` type exported from barrel); added `src/features/profile-sections/` with `ProfileScrollView.tsx` container, 14 section components under `sections/`, barrel at `index.ts`. Section list: HeroBlock (returns null for viewer=other — CandidateHero in 12.4 supplies the other-viewer hero; renders self-preview with full-bleed image + name/age/city-subtitle + chip strip for viewer=self; verified tick wrapped in View with testID since lucide SVG elements don't carry testID props), AboutMeSection, MarriageIntentionsSection, FaithSection (3 real + 5 dummy __dummy_display_only chips), FuturePlansSection, PhotoBlockSection (hides if photos.length < 2), PersonalitySection (reads narrowed preferences.personalityTraits), EducationSection (4 rows with year suffix guards), ProfessionalCareerSection (6 rows guarded independently), ParentsSection (father/mother blocks each guarded on *_name), AddressSection (3 rows), SiblingsSection (hides if siblings empty or undefined; per-card chip guards), VerifiedProfileSection (hides if faceSelfieUri null), ContactActionsSection (returns null for viewer=self; for viewer=other renders phone row + ShareProfileButton variant=row-link + disabled Favourite/Block/Report triad). Test harness at `__tests__/features/profile-sections/_harness.tsx` (includes placeholder describe block to satisfy Jest's "at least one test" requirement); 14 per-section test files (a/b/c assertions each); integration test `ProfileScrollView.hideEmpty.test.tsx` covering all 3 PRD scenarios (42 tests). Non-obvious decisions: (a) country-flag emoji generated via `countryCodeToFlag()` helper using Unicode regional indicator codepoints (U+1F1E0 + offset) — no external package needed, works on all modern iOS/Android; (b) city/country formatted as `MANNHEIM, GERMANY` (uppercase) via `.toUpperCase()` calls on the individual fields before joining; (c) `__dummy_display_only` chips each guard on `dummy?.key !== undefined` not `!= null` — booleans like `smokes: false` must show (not hide) even when false; (d) lucide CheckCircle wrapped in `<View testID="hero-verified-tick">` because SVG roots don't propagate testID props to the RNTL query engine. 198 new tests; 1946 → 2144 pass. tsc: zero errors. Tracking issue #113 closed.
- 2026-08-04: Story 12.4 complete (commit 45dbf4e, branch feat/phase-12-landing-profile). MarriageLandingScreen + CollapsingActionBar + HeaderBar: added `src/features/landing/` with screens/MarriageLandingScreen.tsx, components/CandidateHero.tsx, components/HeaderBar.tsx, components/CollapsingActionBar.tsx, shared/marriageTabBarHidden.ts, barrel index.ts. MarriageLandingScreen composes HeaderBar (sticky, filter left + bell right with red dot on has_unread_notifications) + Animated.ScrollView with CandidateHero as first child + ProfileScrollView viewer=other + floating CollapsingActionBar (4 round buttons: X/Undo/Star/Check) + local Snackbar. Scroll handler writes to marriageTabBarHidden (module-scope Reanimated shared value exported for story 12.6 AppTabs consumer). All 4 action buttons trigger local-state Snackbar with t('landing.actionUnavailable'). Labels landing.* (actionUnavailable, header.filter, header.notifications, hero.activeToday, hero.gold, hero.verified, actions.*) added to EN+UR. Reanimated Jest mock: `@expo/vector-icons` not installed so used lucide-react-native icons (SlidersHorizontal/Bell/X/Undo2/Star/Check) — PRD suggested Ionicons but lucide is the established icon library in this codebase. Jest config key used: `moduleNameMapper` pointing to `__mocks__/react-native-reanimated.js` (a hand-rolled CJS mock with __esModule: true, Animated.ScrollView/View stubs, and addWhitelistedUIProps on the default export for @gorhom/bottom-sheet compat) — the `setupFiles` key still exists (registered jest.setup.ts) but the mock itself lives in moduleNameMapper, not setupFiles, because jest.mock() is unavailable in setupFiles. Had to fight jest-expo: the official `react-native-reanimated/mock.js` imports the native index at module-load (via `./src/mock.ts`) which hits WorkletsModule native init before any mock can intercept — hence the hand-rolled mock. 26 new tests; 2144 → 2170 pass. tsc: zero errors. eslint: zero errors. Tracking issue #115 closed.
- 2026-08-05: Story 12.5 complete (branch feat/phase-12-landing-profile). MenuHomeScreen + MyProfileScreen (Preview tab + Edit shell): added `src/features/profile/components/ProfileThumbnailCircle.tsx` (composes existing Avatar primitive + optional pencil badge via lucide Pencil, pressable wrapper with testID forwarding); added `src/features/profile/screens/MenuHomeScreen.tsx` (Menu-tab home: Marriage-v dropdown no-op + settings/bell no-ops in top bar, profile row with avatar/name/verified-tick/Share-pill/Edit-pill, static chip row for Membership/Invite/Help/Safety, Basic-plan membership card, engagement row with HARD-CODED 15/3/1 counts, €10 Gold discount card — all Get-more/Claim/Upgrade buttons are no-ops); added `src/features/profile/screens/MyProfileScreen.tsx` (two-segment in-screen tab bar Preview|Edit via local activeTab state + useRoute initialTab param, Preview renders ProfileScrollView viewer=self + row-link Share at bottom, Edit renders EmptyState "Coming soon", header: close-X/goBack + verified name centre + icon-only Share right). Added testID prop to `TouchableArea` component (previously lacked it). Labels added: `menu.*` (marriageDropdown, settings, notifications, editPill, membershipCard.*, chips.*, engagement.*) and `myProfile.*` (tabs.preview, tabs.edit, editComingSoon.*, close) to both EN+UR with full parity. Tests: `__tests__/features/profile/MenuHomeScreen.test.tsx` (7 tests: a/b/c/d/e all passing); `__tests__/features/profile/MyProfileScreen.test.tsx` (20 tests: a/b/c/d/e all passing). 2170 → 2197 pass (+27). tsc: zero errors. Non-obvious decisions: (a) useNavigation mocked via jest.mock('@react-navigation/native') rather than full NavigationContainer scaffold — simpler and matches test needs; (b) TouchableArea gained testID prop as additive extension (no breaking change — tests for TouchableArea itself still pass); (c) engagement counts emitted as String(N) inside Heading so getByText('15') resolves correctly; (d) MyProfileScreen renders useRoute initialTab to set useState initial value, making tests that inject initialTab='edit' work without interaction. Tracking issue #116 closed.
- 2026-08-04: Story 12.6 complete (branch feat/phase-12-landing-profile). AppTabs rename + Menu nested stack wiring: updated `src/navigation/AppTabs.tsx` from 4 placeholder tabs (Discover/Requests/Chat/Menu) to 4 production tabs (Marriage/Explore/Chat/Menu) — Marriage renders MarriageLandingScreen, Menu renders new MenuStack, Explore+Chat remain EmptyState placeholders; Marriage tab binds marriageTabBarHidden shared value via useAnimatedStyle (translateY 0→TAB_BAR_HEIGHT + opacity 1→0); Menu tab icon is dummyprofile.photos[0] in a 24×24 circle with Menu lucide fallback on onError; updated `src/navigation/types.ts` (AppTabsParamList renamed Discover→Marriage/Requests→Explore, added NavigatorScreenParams<MenuStackParamList> for Menu, added exported MenuStackParamList); added `src/navigation/MenuStack.tsx` (createNativeStackNavigator with MenuHomeScreen initial + MyProfileScreen, headerShown: false); added `nav.tabs.marriage`/`nav.tabs.explore` to both EN+UR label files; updated `__tests__/navigation/AppTabs.test.tsx` (mocked MarriageLandingScreen + MenuStack as lightweight stubs, removed redundant jest.mock('react-native-reanimated') that overrode moduleNameMapper, swapped assertions from nav.tabs.discover → marriage); updated `__tests__/navigation/auth-gate.test.tsx` (all 9 occurrences of nav.tabs.discover → nav.tabs.marriage, stub renders 'Marriage' instead of 'Discover'); added `__tests__/navigation/MenuStack.test.tsx` (3 E2E wiring tests: initial route is MenuHomeScreen, avatar tap pushes MyProfileScreen, goBack restores MenuHomeScreen). Non-obvious decisions: (a) PRD specified MaterialCommunityIcons/ring for Marriage tab but that library is not installed — used lucide Heart (matrimonial semantic match); (b) PRD specified Ionicons for several tab icons — @expo/vector-icons not installed, all icons are lucide-react-native; (c) removed jest.mock('react-native-reanimated') from AppTabs.test.tsx because calling it overrides the moduleNameMapper entry pointing to the hand-rolled __mocks__/react-native-reanimated.js, causing the native Worklets init to fire; (d) MenuTabIcon uses local useState failed state with Image onError callback for the avatar→Menu fallback. 2197 → 2200 pass (+3). tsc: zero errors. eslint: zero errors. Tracking issue #117 closed.
- 2026-08-04: Phase 12 (Landing page + profile page + profile edit shell) complete. All 6 stories shipped: 12.1 dummy fixtures + types, 12.2 14-section profile catalog with per-chip hide guards, 12.3 buildShareMessage + ShareProfileButton (3 variants), 12.4 MarriageLandingScreen + CollapsingActionBar + HeaderBar + Reanimated scroll-driven tab-bar collapse, 12.5 MenuHomeScreen + MyProfileScreen + ProfileThumbnailCircle, 12.6 AppTabs rename + MenuStack wiring. Tracking issues #112-#117 closed. PR #118 open (feat/phase-12-landing-profile → development). 1934 → 2200 tests (+266 across all 6 stories). Phase-wide non-obvious patterns: (a) Zero backend calls — all data from assets/dummyfemale.json and assets/dummyprofile.json static imports; teardown checklist in context.md → Before shipping; (b) Icon library is lucide-react-native throughout — PRD referenced @expo/vector-icons and MaterialCommunityIcons but those packages are not installed; (c) react-native-reanimated mocking via moduleNameMapper (__mocks__/react-native-reanimated.js hand-rolled CJS) — do NOT use jest.mock('react-native-reanimated', () => require('react-native-reanimated/mock')) as it bypasses moduleNameMapper and hits native Worklets init; (d) marriageTabBarHidden is a module-scope makeMutable shared value — imported by both MarriageLandingScreen (writer) and AppTabs (reader) with no circular dependency because the shared module is in features/landing/shared/; (e) MenuStack test uses real NavigationContainer + real createNativeStackNavigator with stubbed screens — verifies actual stack wiring, not just component rendering.
- 2026-08-26: Story 13.1 complete (commit df92242, branch feat/phase-13-discover). Deck fixtures + DummyDeckProfile type + photo-registry entries: added `src/types/DummyDeckProfile.ts` (combines 16 real backend deck_view columns with 19 frontend-only extension fields, all tagged `@frontend-extension` for grep-ability; includes `__dummy_display_only` block with `is_active_today`, `membership_tier`, `has_unread_notifications`); added `assets/dummydeck/deckFemale2-5.json` (four distinct female fixtures: Nadia Hassan/Amsterdam/Marketing, Sara Mirza/London/Software Engineer, Layla Rahman/Toronto/Dentist, Zainab Ali/Dubai/Architect — each with distinct marital_status, has_children, marriage_time, meet_time; alternates Female3/Female4); added `src/features/discover/data/deckFixtures.ts` exporting `DECK_FIXTURES: ReadonlyArray<DummyDeckProfile>` — Aisha is index 0 via re-import of `assets/dummyfemale.json` (no deckFemale1.json copy created, single source of truth preserved); added `__tests__/features/discover/deckFixtures.test.ts` (23 new tests: structural conformance, uniqueness, photo resolution via resolveDummyPhoto(), AboutMe/MarriageIntentions/Education/ProfessionalCareer section field population, __dummy_display_only block, all 16 backend deck_view fields). `src/assets/dummyPhotoRegistry.ts` unchanged (Female3+Female4 already registered). tsc: zero errors. 2200 → 2223 pass (+23). Pre-existing failures in PhotoBlockSection/VerifiedProfileSection (3 tests) confirmed unchanged from development baseline. Draft PR #120 opened (feat/phase-13-discover → development). Tracking issue #48 closed.
- 2026-08-26: Story 13.2 complete (commit 955d510, branch feat/phase-13-discover). Full-profile fixtures + friendship/request state seed (Mehvish, Qurat): added `assets/dummymehvish.json` (Mehvish Hayat, 26, Hamburg DE, Biomedical Engineer, 2 siblings, Female3.png, 5 personalityTraits, silver tier, all requiredForCompletion fields populated) and `assets/dummyqurat.json` (Qurat Baloch, 28, Munich DE, Solicitor, 1 sibling, Female4.png, 5 personalityTraits, gold tier, all fields populated). Added `assets/dummyfriendships.json` (seed: Mehvish user_id) and `assets/dummyrequests.json` (seed: one pending request from Qurat → current user). Added `src/types/DummyFullProfile.ts` — viewer-neutral type extending UserProfile with same shape as DummyOwnProfile; exports `DummyOwnProfile = DummyFullProfile` as backwards-compat alias (DummyOwnProfile.ts itself unchanged — no phase-12 callers touched). Added `src/state/friendship/FriendshipProvider.tsx` with module-scope `ALL_FULL_PROFILES` registry (NG1/Q15, grep-tagged TODO(mock-only)), `useFriendship()` hook exposing friends/requests/acceptRequest/declineRequest/isFriend/receivedRequestFrom/getFullProfile; acceptRequest defensive no-op + console.warn when userId not in registry; in-memory state resets on cold start (expected per Note 2). Mounted `FriendshipProvider` in `App.tsx` inside `OnboardingCompletionProvider` wrapping `NavigationContainer`. 53 new tests: 31 in `__tests__/assets/friendFixtures.test.ts` + 22 in `__tests__/state/friendship/FriendshipProvider.test.tsx`. 2223 → 2275 pass (+52; delta accounts for test framework overhead). Pre-existing PhotoBlockSection/VerifiedProfileSection failures (3 tests) unchanged. tsc: zero errors. Tracking issue #49 closed.
- 2026-08-26: Story 13.4 complete (branch feat/phase-13-discover). OtherProfileScreen — full profile behind back-arrow header + access gate: added `src/features/profile/components/BackHeaderBar.tsx` (back-arrow-only header, same paddings/shadow/safe-area as HeaderBar, 40×40 icon button, reusable in phases 15/17/18); added `src/features/profile/screens/OtherProfileScreen.tsx` (access guard B1/Q7 — friend-source requires isFriend, request-source requires receivedRequestFrom||isFriend to survive 1500ms Accept window; profile resolved via getFullProfile(userId) per NG1/Q15; Accept = acceptRequest+snackbar+1500ms goBack; Decline = declineRequest+setPendingToast+immediate goBack per NG2/Q16); extended `ProfileScrollView` with optional `contactVisible?: boolean` prop (defaults to true — phase-12 callers unaffected; OtherProfileScreen passes contactVisible={source==='friend'} to hide phone/share/action-triad when viewing request-sender); extended `FriendshipProvider` with `pendingToast/setPendingToast/consumePendingToast` (option b handoff mechanism — ExploreHomeScreen story 13.5 consumes on focus); added `testID` prop to `Snackbar` component; added `otherProfile.*` labels (7 keys) to both `labels.en.json` and `labels.ur.json` with full parity. Tests: BackHeaderBar (6), ProfileScrollView.contactVisible (6), OtherProfileScreen (21), FriendshipProvider pendingToast (4) = 37 new tests. 2310 → 2344 pass (+34 net; 3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. Tracking issue #51 closed.
- 2026-08-26: Story 13.5 complete (branch feat/phase-13-discover). ExploreStack wiring + ExploreHomeScreen Friends/Requests subtabs: added `src/navigation/ExploreStack.tsx` (createNativeStackNavigator with ExploreHomeScreen initial + OtherProfileScreen, headerShown: false — mirrors MenuStack pattern); added `ExploreStackParamList` to `src/navigation/types.ts` (ExploreHomeScreen: undefined, OtherProfileScreen: { userId: string, source: 'friend'|'request' }); updated `AppTabsParamList.Explore` to `NavigatorScreenParams<ExploreStackParamList>`; replaced inline `ExploreScreen` EmptyState placeholder in `AppTabs.tsx` with `ExploreStack` (mirrors Menu→MenuStack pattern); updated `OtherProfileScreen.tsx` to use `RouteProp<ExploreStackParamList, 'OtherProfileScreen'>` — deleted local `OtherProfileRouteParams` placeholder type from story 13.4; added `src/features/explore/screens/ExploreHomeScreen.tsx` (header title-only with safe-area padding; two-segment Friends|Requests tab bar mirroring MyProfileScreen Preview/Edit pattern; Friends FlatList with ProfileThumbnailCircle size=48 + Heading rows tappable to OtherProfileScreen source='friend'; Requests FlatList with sibling-structure rows — TouchableArea on left column + sibling Row of Accept/Decline buttons to avoid touch propagation firing both navigate+action; Accept: acceptRequest+Snackbar; Decline: declineRequest+Snackbar; empty states for both tabs; useFocusEffect consuming FriendshipProvider.pendingToast on screen-focus for cross-screen Decline handoff from OtherProfileScreen); added `explore.*` labels (11 keys) to both `labels.en.json` and `labels.ur.json` with full parity. Tests: ExploreStack.test.tsx (3 navigation-wiring E2E tests), ExploreHomeScreen.test.tsx (13 tests covering all 7 ACs). 2344 → 2360 pass (+16; 3 pre-existing PhotoBlockSection/VerifiedProfileSection failures unchanged). tsc: zero errors. Non-obvious decisions: (a) sibling-structure touch isolation in Requests rows (Note 1 in PRD) — TouchableArea wraps only the left column, Accept/Decline buttons in a sibling Row with no onPress conflict; (b) useFocusEffect mocked as useEffect in tests (calls callback immediately on mount) — consistent with OtherProfileScreen test pattern; (c) OtherProfileScreen route type promotion: deleted local placeholder type, imported ExploreStackParamList — no behavior changes to the screen; (d) AppTabsParamList.Explore changed from `undefined` to `NavigatorScreenParams<ExploreStackParamList>` — AppTabs.test.tsx unaffected because it stubs screen components. Tracking issue #52 closed.
- 2026-08-26: Story 13.3 complete (commit 613655f, branch feat/phase-13-discover). MarriageLandingScreen → deck-of-condensed-cards refactor: extracted `CandidateHeroProfile` structural interface from `CandidateHero.tsx` (B2/Q8) so both `DummyFemaleProfile` and `DummyDeckProfile` satisfy it without a cast; widened `CollapsingActionBar` with per-button handlers (onPass/onUndo/onSuperLike/onLike) alongside legacy `onAction` fallback for backwards compatibility; added `src/features/landing/components/DeckCard.tsx` (CandidateHero + 4 sections: AboutMe, MarriageIntentions, Education, ProfessionalCareer — 10 sections excluded, cast `deck as unknown as UserProfile & DummyOverlay` at DeckCard boundary per PRD pattern); refactored `MarriageLandingScreen` with deck index state (starts at 0), per-button handlers (Like = mock theatre + likeSent snackbar + TODO(mock-only) comment, Dislike = advance only, Undo = bounded decrement, Star = actionUnavailable), empty state + `CollapsingActionBar` absent when exhausted (S3/Q11), `marriageTabBarHidden.value` + `scrollTo({y:0})` reset in single `useEffect` keyed on `currentDeckIndex` (S4/Q12), HeaderBar unread-dot from module-scope `CURRENT_USER_HAS_UNREAD` derived from `dummyprofile.json` not deck card (S1/Q9); added `landing.likeSent`, `landing.deckExhausted.title`, `landing.deckExhausted.description` to both `labels.en.json` and `labels.ur.json`. Tests: 4 new test files (CandidateHero.deck: 5, DeckCard: 15, MarriageLandingScreen.deck: 10, MarriageLandingScreen.bellDot: 2); existing MarriageLandingScreen.test.tsx updated to phase-13 behavior (26 tests). 2275 → 2310 pass (+35). Pre-existing 3 failures (PhotoBlockSection/VerifiedProfileSection) unchanged. tsc: zero errors. Non-obvious decisions: (a) bell-dot mock test split into separate file (MarriageLandingScreen.bellDot.test.tsx) that uses top-level `jest.mock('../../../assets/dummyprofile.json', ...)` — `jest.resetModules()` in beforeAll caused duplicate React instances and invalid hook calls; (b) `DECK_FIXTURES[currentDeckIndex]!` non-null assertion + eslint-disable comment to satisfy TypeScript array index narrowing since the `!isExhausted` guard isn't visible to tsc at the array access site. Tracking issue #50 closed.
- 2026-08-26: Phase 13 (Discover — deck landing, Explore Friends+Requests, gated full-profile view) complete. All 5 stories shipped: 13.1 deck fixtures + `DummyDeckProfile` type (Aisha re-exported, 4 new females), 13.2 Mehvish+Qurat full-profile fixtures + `FriendshipProvider` with in-memory state and `ALL_FULL_PROFILES` registry backing `getFullProfile()`, 13.3 `MarriageLandingScreen` deck refactor (DeckCard, `CandidateHeroProfile` widening, mock-theatre Like, hide-on-exhausted, tabbar reset, bell-dot from current user), 13.4 `OtherProfileScreen` + reusable `BackHeaderBar` + `ProfileScrollView.contactVisible?` prop + broadened access guard + `FriendshipProvider.pendingToast` cross-screen Decline handoff, 13.5 `ExploreStack` + `ExploreHomeScreen` with Friends/Requests subtabs (sibling touch-scoping) + AppTabs rewired to nested stack + `useFocusEffect` consuming pendingToast. Tracking issues #48–#52 all closed. PR #120 open (feat/phase-13-discover → development). 2200 → 2360 tests (+160 across all 5 stories). Phase-wide non-obvious patterns: (a) **Zero backend calls, zero native modules** — no `expo run:android` reinstall required on device 000323572000090; Metro reload picks up the merge; (b) all mock surfaces marked `TODO(mock-only)` — teardown checklist added to phase-13 PRD `teardown_additions:` block (wipe deck+friendship fixtures, replace `useFriendship()` with real GraphQL/REST, rewire `DECK_FIXTURES` to `useDeckQuery()`, drop `@frontend-extension` fields from `DummyDeckProfile` when backend `deck_view` catches up); (c) `getFullProfile(userId)` on `FriendshipProvider` is the single source of truth for user_id → DummyFullProfile lookup — no ad-hoc fixture imports at OtherProfileScreen or ExploreHomeScreen; (d) Decline uses `FriendshipProvider.pendingToast` for cross-screen handoff (Explore consumes on focus) instead of route params, because after `declineRequest()` the user is neither friend nor pending-request-sender, so any goBack delay would trip the broadened access guard and paint EmptyState on top of the toast — same class of bug B1 fixed for Accept; (e) `CandidateHero` prop widened to a co-located `CandidateHeroProfile` structural interface (both `DummyFemaleProfile` and `DummyDeckProfile` satisfy it with no cast); (f) sibling-structure touch isolation in ExploreHomeScreen Requests rows — TouchableArea on left column only, buttons in sibling Row — avoids RN's default propagation firing both navigate + action; (g) bell-dot mock test isolated in its own file because `jest.resetModules()` creates duplicate React instances incompatible with RNTL's render. 3 pre-existing baseline failures (PhotoBlockSection/VerifiedProfileSection) on `development` unchanged throughout the phase — no regression introduced. Two brainstorm passes ran before dispatch: brainstorm-1 surfaced B1/B2 blockers + S1–S5 sharpens + Note 1/4 UX; brainstorm-2 (post-answers) surfaced NG1 (getFullProfile registry) and NG2 (immediate Decline goBack); QA/explanations.txt captured all in layman-style with analogies + fix options for user selection. Existing GitHub issues #48–#52 (from an earlier scrapped phase-13 plan) repurposed by rewriting titles + bodies rather than closing and re-opening — preserved board-card lineage.
