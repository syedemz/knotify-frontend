# knotify-frontend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Not yet started. Awaiting `architecture.md` and `/create-plan`.

## Active blockers
None. Phase 1 gates §17.22 and §17.26 both resolved on 2026-07-19.

## Before shipping

Local auth mock mode is active while the AWS backend is unspun (to save cost during UI walkthrough). Before the first real production build, delete the mock scaffolding in this order:

1. **`src/config/env.ts`** — delete the `isMockAuth` field from the exported `env` object (currently keyed off the sentinel `cognito.userPoolId === 'eu-central-1_dummydummy'`).
2. **`src/services/auth/cognitoClient.ts`** — delete every `if (env.isMockAuth) { ... }` branch (currently in `ensureConfigured`, `signUp`, `confirmSignUp`, `signIn`, `signOut`, `refreshSession`) and delete the `makeMockTokens` helper.
3. **`src/config/backendConfig.dev.json`** — replace the dummy sentinel values (`eu-central-1_dummydummy`, `dummydummydummydummydummy1`, `dummy.example.invalid`, `dummy.appsync-api...`, `dummy.cloudfront.net`) with the real values emitted by the backend Terraform outputs. `env.ts` throws at import time on any `<FILL_*>` placeholder, so partial fills are safe.

After all three are done, `grep -r isMockAuth src/` should return zero hits and `grep -r dummydummy src/config/` should return zero hits. Add a CI grep gate at that time if you want a hard stop.

## Critical design decisions
None yet.

## Recent changes
- 2026-07-21: Story 1.8 complete.
- 2026-07-21: Story 1.9 complete (commit 7699f91). Navigation skeleton: src/navigation/ with RootNavigator (auth-gate §6.2), AuthStack (Login/ForgotPassword/ResetPassword placeholders), OnboardingStack (Page01-Page31 placeholders), AppTabs (Discover/Requests/Chat/Menu placeholders), linking.ts (empty config), types.ts (4 ParamList types). App.tsx updated: NavigationContainer + RootNavigator wired, GestureHandlerRootView + SafeAreaProvider added (fulfills TODO(app-root) from 1.6). 4 nav.tabs label keys added with Urdu parity. 21 new tests in 4 files; 648/648 pass, coverage 83.63%/83.89%/85.15%/83.62% (all above threshold). tsc: zero errors. state/ providers scaffolded: LanguageProvider (locale/setLocale, AsyncStorage persist under app.locale, device-locale default, confirm-then-reload via Alert.alert per §17.26: persist→forceRTL→reloadAsync on accept, no-op on cancel), QueryProvider (staleTime 5m, retry 2, gcTime 10m, mutation retry 0 per §7.3), AuthProvider (status/session/profileComplete/signIn/signOut/refresh per §7.2, silent-refresh on mount, signOut clears TanStack Query cache). Rewired: useLocalizedFontFamily now reads from @/labels getActiveLocale() (module-level store updated by LanguageProvider via setActiveLocale()); labels/index.ts exports setActiveLocale()/getActiveLocale() — no circular deps. App.tsx provider order: fonts→ThemeProvider→LanguageProvider→QueryProvider→AuthProvider→placeholder. Installed: expo-updates, @react-native-async-storage/async-storage (npx expo install), @tanstack/react-query@5, expo-localization. jest.config.js: added @react-native-async-storage/async-storage mock mapping. 41 new tests (3 files); full suite 627/627 pass, coverage 83.16%/83.74%/86.41%/83.13% (all above threshold). tsc --noEmit: zero errors.
- 2026-07-21: Story 1.10 complete — Phase 1 done. .github/workflows/ci.yml created: triggers on pull_request→development and pull_request→main; runs npm ci, npx tsc --noEmit, npx eslint src __tests__, npx jest --coverage (includes labels-parity test). ESLint 9 flat config at eslint.config.js: @typescript-eslint/parser@8 + @typescript-eslint/eslint-plugin@8 + eslint-plugin-react@7 + eslint-plugin-react-hooks@7; minimal rules, zero errors on existing codebase (16 warnings only). Coverage thresholds (lines 80 / branches 75 / functions 80 / statements 80) enforced by jest.config.js, surfaces as non-zero jest exit on failure. cicd.md updated with workflow path, four commands, and coverage table. 648/648 tests pass; all coverage thresholds met (83.63%/83.89%/85.15%/83.62%). tsc: zero errors. eslint: zero errors.
- 2026-07-23: Story 2.1 complete (commit 160d51f). WizardHeader, WizardProgress, WizardFooter chrome components added to src/components/; stub src/features/onboarding/pageMap.ts with TOTAL_PAGES=31 (story 2.2 augments); wizard.header.back / wizard.footer.continue / wizard.footer.back labels added with Urdu parity; jest.config.js moduleNameMapper routes lucide-react-native to CJS build (fixes ESM parse failure for transitive imports); theme.md §9.16-9.18 entries; 51 new tests, 699/699 pass, coverage 84.05%/84.39%/85.66%/84.05%; tsc zero errors. PR #81 opened (phase-2 branch feat/phase-2-wizard-shell-b1 → development).
- 2026-07-23: Story 2.2 complete (commit f46d6e9). UserProfile/UserProfileWritable types from db-schema.json; OnboardingDraft+SiblingDraft+createEmptyDraft in draftSchema.ts; firstCheckpoint/secondCheckpoint constants; PAGE_MAP (31 routes→pages), OnboardingRouteName type, TOTAL_PAGES=31 in pageMap.ts; useOnboardingDraft (200 ms trailing-edge debounce, checkpoint non-regression via advanceWithCheckpoint); useOnboardingProgress (throws on unknown route); useCheckpointResume (3 checkpoint states); 50 new tests, 749/749 pass, coverage 85.53%/84.46%/86.20%/85.68%; tsc zero errors.
- 2026-07-23: Story 2.3 complete. OnboardingStack routes renamed from Page01..Page31 to 31 semantic names matching PAGE_MAP; types.ts OnboardingStackParamList uses mapped type from OnboardingRouteName (no drift); useCheckpointResume wired as initialRouteName in OnboardingStack; RootNavigator auth-gate updated: unauthenticated → OnboardingStack (not AuthStack); auth-gate.test.tsx updated; Pages 1-4 concrete placeholder screens created (Screen + WizardHeader hideProgress + Text placeholder); OnboardingStack.test.tsx updated with PAGE_MAP equality guard + checkpoint-resume wiring tests; linking.ts no-op verified; 18 new tests, 767/767 pass, coverage 85.19%/84.46%/85.23%/85.33%; tsc zero errors.
- 2026-07-23: Story 2.4 complete (commit 4da2e7b). Page01WelcomeScreen implemented: full-screen ScreenBackground (new catalog component), logo + Knotify title (Heading display.md), globe icon opens BottomSheet with English/Urdu language options delegating to useLocale().setLocale(), Continue with email → Page02EmailScreen, Continue with Google no-op. 6 new label keys in labels.en.json + labels.ur.json (onboarding.welcome.* + onboarding.language.*). Stale BottomSheet TODO(app-root) comment deleted. 790/790 tests pass, coverage 85.74/84.83/85.99/85.87; tsc zero errors. Note: hook is useLocale() (not useLanguage() as named in AC — useLanguage does not exist in codebase; useLocale is the correct export from LanguageProvider).
- 2026-07-23: Story 2.5 complete (commit 46cb28d). src/Helper/ folder created with validationHelper.ts (isValidEmail, isSixDigitCode, passwordMeetsCognitoPolicy) and errorHelper.ts (cognitoErrorToLabelKey). SecureStorageKey.OnboardingBootstrapPassword added to secureStorage.ts. Page02EmailScreen fully implemented: email+password fields (textContentType/autoComplete OS hints), secure-store-write BEFORE signUp ordering, best-effort delete on signUp failure, inline field errors, API error rendering. TextInput and PasswordInput extended with textContentType/autoComplete/autoCapitalize/autoCorrect props. 15 onboarding.email.* labels with Urdu parity. 77 new tests (41 validationHelper + 16 errorHelper + 20 screen wiring); 868/868 pass, coverage 86.45/85.18/86.43/86.65; tsc zero errors. Decision: cognitoClient.signUp called with {username: email, password, email} since Cognito username_attributes=["email"] makes username IS the email (per main.tf).
- 2026-07-23: Story 2.6 complete (commit ccaaf0d). Page03ConfirmCodeScreen fully implemented: 6-digit code TextInput (number-pad, maxLength=6), double-tap protection (loading+disabled for entire confirmSignUp→secure-store-read→signIn→secure-store-delete chain, resolution F), routes sign-in through useAuth().signIn so AuthProvider flips to authenticated. Error paths: CodeMismatchException, ExpiredCodeException, LimitExceededException, signInAfterConfirm, bootstrapMissing, generic. 9 confirmCode label keys with Urdu parity. 22 new wiring tests; pages1to4Screens.test.tsx updated to mock useAuth and test real p3 content; 890/890 pass, tsc zero errors. Decision: subtitle interpolation uses runtime .replace('{email}', email) since t() resolver lacks interpolation support.
- 2026-07-23: Story 2.7 complete (commit 73a58ee) — Phase 2 done. Page04GetStartedScreen implemented: images.onboarding.banner centered via Column+Image catalog components, WizardFooter with continueLabel='Get started' (always enabled), tap navigates to Page05SexScreen with no checkpoint advance. onboarding.getStarted.button label added with Urdu parity. 9 new wiring tests; pages1to4Screens.test.tsx updated from placeholder to real-content assertions; 900/900 pass, coverage 86.78/84.94/86.41/87.07 (all above threshold); tsc zero errors.
- 2026-07-23: Phase 2 (Wizard shell + pages 1-4) complete. All 7 stories (2.1-2.7) shipped: wizard chrome (WizardHeader/Progress/Footer), draft store + pageMap + checkpoint hooks, 31-route OnboardingStack with semantic names + auth-gate flip, Page 1 Welcome + language BottomSheet, Page 2 email/password sign-up, Page 3 confirm code + auto-signIn via useAuth, Page 4 Get started. Tracking issues #74-#80 closed. PR #81 open (feat/phase-2-wizard-shell-b1 → development). Thin index flipped done: true, last_updated: 2026-07-23. Non-obvious decisions: (a) Cognito signUp signature is {username, password, email} with username=email per user_pool config; (b) Page 3 routes through useAuth().signIn (not cognitoClient.signIn) so AuthProvider status flips and RootNavigator gate opens; (c) labels resolver lacks interpolation — runtime .replace('{email}', email) used for Page 3 subtitle; (d) hook is useLocale() (not useLanguage()); (e) secureStorage method is .del (not .remove); (f) lucide-react-native ESM main entry required jest moduleNameMapper to CJS build.
- 2026-07-23: Story 3.1 complete (commit de195bd). Page05SexScreen: two SelectionTile components (Male/Female) sourced from options.gender (never inlined); tile tap writes sex to draft via update(), calls advance(6), navigates to Page06NameScreen; no Continue button; both tiles always tappable (back-nav editable). SelectionTile new catalog component added to src/components/ with testID, selected state (brand-color border), accessible={false} on inner image to prevent duplicate a11y label. DraftFields type introduced in draftSchema.ts to tighten sex field to 'Male' | 'Female' (excludes null); useOnboardingDraft.update() signature updated from Partial<UserProfileWritable> to DraftFields. src/Helper/immutableFieldHelper.ts introduced with isImmutable() predicate (sex/first_name/last_name/birthday/religion/subsect → true; username/job_title/email → false). Labels: onboarding.sex.male + onboarding.sex.female with Urdu parity. 34 new tests (10 immutableFieldHelper + 9 SelectionTile + 15 Page05SexScreen); 934/934 pass; coverage 85.18/83.56/86.64/85.54; tsc zero errors; eslint zero errors. Tracking issue #82 closed.
- 2026-07-23: Story 3.2 complete (commit fc1e1ff). Page06NameScreen: two TextInput fields (first_name, last_name, maxLength=35) with OS autofill hints (givenName/familyName textContentType, given-name/family-name autoComplete); Continue disabled until both fields pass isValidName(); on Continue generates username via generateUsername(), writes all three fields to draft, advances to page 7, navigates to Page07BirthdayScreen. isValidName() added to validationHelper.ts: allows [A-Za-z], space, hyphen, apostrophe; rejects leading/trailing whitespace; length 1–35. src/Helper/usernameHelper.ts created: sanitizeNamePart() (NFD-normalize + strip non-[a-z] for diacritic removal) + generateUsername() (sanitized first+last + 4-digit padded suffix). Labels: onboarding.name.* (title, firstNameLabel/Placeholder, lastNameLabel/Placeholder, errors.invalidName) with Urdu parity. 49 new tests (12 isValidName, 8 sanitizeNamePart, 10 generateUsername, 19 Page06NameScreen wiring); 983/983 pass; coverage 85.38/83.96/86.62/85.72 (all above threshold); tsc zero errors; eslint zero errors. Tracking issue #83 closed.
- 2026-07-23: Story 3.3 complete (commit ba47dea). Page07BirthdayScreen: banner image + catalog DatePicker (min=1900-01-01, max=today) + live age preview via agePreview label key ({age} years interpolation) + inline error from validateBirthday() LabelKey + Continue gate → Page08FirstCheckpointScreen. src/Helper/dateHelper.ts created: isAtLeast18, isNotFuture, isYearReasonable, age, validateBirthday (all with injected-today for test determinism). 7 birthday label keys with Urdu parity (title, pickerLabel, agePreview, errors.under18/future/yearUnreasonable/invalid). 46 new tests (26 dateHelper boundary tests, 20 Page07BirthdayScreen wiring tests); 1029/1029 pass; coverage 85.56/83.9/86.47/85.97 (all above threshold); tsc zero errors; eslint zero errors. Tracking issue #84 closed.
- 2026-07-23: Story 3.4 complete (commit fa25b28). Page08FirstCheckpointScreen: banner image + Start button (always enabled, no taglines) calls advanceWithCheckpoint(9, 'firstCheckpoint') then navigates to Page09ReligionSubsectScreen; WizardHeader hideProgress (mirrors page 4 pattern). OnboardingStack: real Page05-08 screen components wired (placeholders removed for pages 5-8). Labels: onboarding.firstCheckpoint.title + onboarding.firstCheckpoint.start with Urdu parity. 11 wiring tests (mount, Start enabled, advanceWithCheckpoint call order + args, navigation, call ordering, resolveCheckpoint non-regression invariant, WizardHeader back); 8 integration tests in checkpointResume.integration.test.tsx (real useCheckpointResume, seeded useOnboardingDraft mock, asserts Page09 as initial route + all phase-3 draft fields via DraftReader consumer). 19 new tests; 1048/1048 pass; coverage 85.62/83.9/86.59/86.04 (all above threshold); tsc zero errors; eslint zero errors. Tracking issue #85 closed.
- 2026-07-23: Phase 3 (Identity — sex, name, birthday, first checkpoint, pages 5-8) complete. All 4 stories (3.1-3.4) shipped: Page 5 sex tile picker (SelectionTile, auto-advance, editable on back-nav), Page 6 name inputs + client-side username generation (isValidName, generateUsername, sanitizeNamePart), Page 7 birthday DatePicker + age preview + validateBirthday (dateHelper with injected-today), Page 8 first checkpoint marker (advanceWithCheckpoint → Page09, checkpoint-resume integration test). Introduced: SelectionTile catalog component, DraftFields narrowed type (sex: 'Male'|'Female'), immutableFieldHelper.ts, usernameHelper.ts, dateHelper.ts, first-checkpoint labels. OnboardingStack now wires real screens for pages 1-8. Tracking issues #82-#85 closed. PR #86 open (feat/phase-3-identity-b2 → development). Non-obvious decisions: (a) DraftFields.sex narrowed from 'Male'|'Female'|null to 'Male'|'Female' (null excluded from draft); (b) username generated on page 6 Continue (not lazily); (c) birthday validation uses injected today parameter for test determinism; (d) checkpoint-resume integration test mocks useOnboardingDraft synchronously (not expo-secure-store) because initialRouteName is set on first render before async storage loads.
- 2026-07-24: Story 4.1 complete (phase-4 branch feat/phase-4-religion-work-b3). Page09ReligionSubsectScreen implemented: religion list (options.religion via ListRowSelectable), Islam-only subsect branch (options.islamicSubsect, slides in below), non-Islam auto-advance with subsect="Not Applicable", re-tap no-op guard, religion-switch subsect clear. Labels onboarding.religion.title + onboarding.religion.subsectTitle added with Urdu parity. OnboardingStack wired (placeholder replaced). OnboardingStack.test.tsx and checkpointResume.integration.test.tsx updated to use real screen title ("What is your religion?") instead of placeholder text. 25 new tests in Page09ReligionSubsectScreen.test.tsx; 1073/1073 pass; coverage 85.92/84.34/86.68/86.32 (all above threshold); Page09ReligionSubsectScreen.tsx at 100% coverage. tsc: zero errors; eslint: zero errors. Non-obvious: islamicSubsect.json and religion.json both contain "Prefer not to say" — test uses getAllByLabelText for that subsect entry to avoid "found multiple elements" failure.
- 2026-07-24: Story 4.2 complete (feat/phase-4-religion-work-b3). Page10ProfessionalCategoryScreen implemented: professional category list (options.professionalCategory via ListRowSelectable), tap writes professional_category to draft and auto-advances to Page11WorkDetailsScreen; no Continue button. Label onboarding.professionalCategory.title added with Urdu parity. OnboardingStack wired (placeholder replaced). 16 new tests in Page10ProfessionalCategoryScreen.test.tsx; 1089/1089 pass; coverage 86.04/84.34/86.86/86.44 (all above threshold); Page10ProfessionalCategoryScreen.tsx at 100% coverage. tsc: zero errors; eslint: zero errors.
- 2026-07-24: Story 4.3 complete (feat/phase-4-religion-work-b3). Page11WorkDetailsScreen implemented: 5-field all-required form with employment_type (Select/options.employmentType), job_title (TextInput), employer_name (TextInput), office_address (multiline TextInput), salary_range (Select/options.salaryRange). TextInput extended with multiline+numberOfLines props. Validation helpers isValidJobTitle (max 40), isValidEmployerName (max 50), isValidOfficeAddress (max 150) added to validationHelper.ts with exported max-length constants. 21 label keys (title + 5 labels + 5 placeholders + 10 error keys) added to labels.en.json and labels.ur.json with Urdu parity. Inline per-field errors mirror Page02 pattern (touched flag guards). Continue writes all 5 values to draft and advances to Page12EducationLevelScreen. OnboardingStack placeholder replaced with real screen. db-schema confirmed: all columns TEXT (unbounded), PRD target values 40/50/150 used as-is. 68 new tests (48 validation helper boundary+char-rule tests + 20 screen wiring tests); 1157/1157 pass; coverage 86.9/84.91/87.46/87.25 (all above threshold). tsc: zero errors; eslint: zero errors.
- 2026-07-24: Phase 4 (Religion + work, pages 9-11) complete. All 3 stories (4.1-4.3) shipped: Page 9 religion + dynamic Islam-only subsect branch (ListRowSelectable, same-religion-retap no-op, forward-nav religion-switch clears subsect), Page 10 professional category auto-advance list (test asserts count via options.professionalCategory.length, back-nav overwrites draft), Page 11 5-field work details form (employment_type + salary_range via catalog Select/BottomSheet, job_title + employer_name + office_address via TextInput, inline per-field errors, character rule accommodates P&G/AT&T/Macy's/O'Reilly). Introduced: validationHelper additions (isValidJobTitle, isValidEmployerName, isValidOfficeAddress with exported max-length constants), TextInput multiline+numberOfLines props. 109 new tests total; 900 → 1157 (all pass); coverage 86.9/84.91/87.46/87.25. Tracking issues #22-#24 closed. PR #88 open (feat/phase-4-religion-work-b3 → development). Thin index flipped done: true, last_updated: 2026-07-24. Non-obvious decisions: (a) IMMUTABLE_FIELDS is phase-11 PATCH-builder metadata only — draft stays editable in-session (resolved audit contradiction); (b) db-schema.json declares all string columns TEXT (unbounded) — frontend imposes 40/50/150; (c) TextInput.maxLength set to max+1 so users can type over the limit and see the "tooLong" inline error rather than a silent hard-stop; (d) office_address has no character restriction (freeform for `/`, `#`, apartment names); (e) autoCorrect=true on office_address, false on job_title/employer_name.
- 2026-07-24: Story 5.1 complete (commit ed42be5, branch feat/phase-5-education-b4). Page12EducationLevelScreen implemented: education level list (options.educationLevel via ListRowSelectable, matches page-9 religion picker pattern), tap writes education_level to draft via update() and auto-advances to Page13EducationCredentialsScreen via advance(13)+navigate(); re-tap no-op guard; back-nav restores prior selection from draft via getDraft().fields.education_level. Label onboarding.education.title added with Urdu parity. OnboardingStack placeholder replaced with real screen. 20 wiring tests; 1182/1182 pass; Page12 at 100% coverage; all thresholds met (87.04/85.05/87.62/87.38). tsc: zero errors.
- 2026-07-24: Story 5.2 complete (commit 74eb042, branch feat/phase-5-education-b4). Page13EducationCredentialsScreen implemented: branch-matrix rendering per §11.2.3 (5 education levels), text validators isValidEducationText (max 40, [A-Za-z0-9 \-.,&'] with apostrophe) and isValidEducationYear (4-digit, 1950–currentYear) added to validationHelper.ts, 22 label keys (title + 7 field-sets × label/placeholder/error) in EN/UR, back-nav downgrade reconciliation via useEffect (force-resets irrelevant fields to sentinels), Continue → Page14SecondCheckpointScreen. db-schema: all education text columns are TEXT (unbounded) — 40-char limit is UI-only. 72 new tests (60 validationHelper boundary + 12 screen wiring, 3 AC it-blocks); 1254/1254 pass; coverage 86.57/84.14/86.82/87.46. tsc: zero errors; eslint: zero errors.
- 2026-07-24: Story 5.3 complete (branch feat/phase-5-education-b4). Page14SecondCheckpointScreen: expo-location installed + app.json iOS/Android location permissions, src/services/permissions/index.ts (requestNotificationPermission/requestLocationPermission), expoPush.ts refactored to import from @/services/permissions, useOnboardingDraft extended with setNotificationPermissionStatus/setLocationPermissionStatus (modelled on setSiblings), Page14 three-state mode (loading/prompt/continue) with mount-reconciliation useEffect calling Notifications.getPermissionsAsync + Location.getForegroundPermissionsAsync in parallel, 4 secondCheckpoint label keys EN/UR. All existing mocks updated. 31 new tests (8 permissions unit + 17 Page14 wiring + 6 useOnboardingDraft setters); 1285/1285 pass; coverage 87.17/84.43/87.38/88.2 (all above threshold). tsc: zero errors; eslint: zero errors.
- 2026-07-24: Phase 5 (Education + second checkpoint, pages 12-14) complete. All 3 stories shipped. Tracking issues #25, #26, #27 closed. PR opened (feat/phase-5-education-b4 → development).
- 2026-07-25: Dev workflow shifted from Expo Go to Android **development build** (PR #91 merged). `npm run android`/`ios` now run `expo run:android`/`ios` (was `expo start --android`/`ios`). Trigger: Page 14's `expo-notifications` push-token call crashes Expo Go in SDK 53+ ("remote notifications functionality removed"). CNG kept — `android/`, `ios/` stay gitignored; `app.json` authoritative. Requires Temurin JDK 17 at `C:/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot` (Android Studio's bundled JBR is Java 11; Gradle 9.3.1 rejects it). Build+install via `npx expo run:android` onto `Pixel_6_Pro_API_34` (`emulator-5554`); Metro on `localhost:8081` hot-reloads JS/TS edits (no rebuild); `npx expo prebuild` only needed after `app.json` plugin/native changes. Reset onboarding state on emulator with `adb shell pm clear com.knotify.app` (wipes secure-store without reinstalling). Do NOT test via Expo Go / QR code / `expo start`.
- 2026-07-25: Onboarding draft store refactored to Context/Provider (PR #92 merged). Root cause of Page 13 empty-render bug: `useOnboardingDraft` was a plain hook, so each screen owned its own state+ref instance — Page 12 wrote `education_level` to its instance and auto-advanced before the 200ms debounced SecureStore write landed, so Page 13 mounted with a fresh empty draft and `visibleFields(null)` returned no fields. Fix: `src/features/onboarding/hooks/useOnboardingDraft.tsx` (renamed from `.ts`) now exports `OnboardingDraftProvider` (owns single state instance via internal `useOnboardingDraftState`) and consumer `useOnboardingDraft` (throws if not inside provider). `OnboardingStack` split into outer wrapper (mounts provider) + inner navigator (consumes draft for `useCheckpointResume`+`isLoading` gate). All 15 test `jest.mock` blocks added `OnboardingDraftProvider: ({ children }) => children` passthrough; `useOnboardingDraft.test.ts` wraps `renderHook` with `wrapper: OnboardingDraftProvider`. 1285/1285 tests pass. Verified on emulator: after `adb shell pm clear`, walked Page 1 → 12 → 13, credential fields render.
