# knotify-frontend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Not yet started. Awaiting `architecture.md` and `/create-plan`.

## Active blockers
None. Phase 1 gates §17.22 and §17.26 both resolved on 2026-07-19.

## Critical design decisions
None yet.

## Recent changes
- 2026-07-19: architecture.md — cross-phase decisions locked in ahead of `/create-plan`. §11.1 auth: Register + ConfirmEmail removed from `AuthStack`, sign-up bootstrap consolidated into `OnboardingStack` pages 2–3. §5 folder layout + §6.1 nav graph updated to match. New §13.5 "Phase decomposition policy" codifies: phase 1 owns CI + i18n scaffold + Urdu font wiring; onboarding batches B1–B10 follow; post-onboarding features (§11.3–§11.10) get thin phases now with design deferred to `/implement-phase` brainstorms; push registration lives in the settings phase; deep-link route table filled by chat + settings phases; iOS validation deferred per phase; MSW fixture parity + labels.en/ur parity are per-phase acceptance criteria. §17.8 resolved (iOS tooling absent, deferred). §17.22 (Urdu font) + §17.26 (RTL reload UX) marked as gating phase 1.
- 2026-07-19: /create-plan run. Thin `implementationplan.md` index generated with 19 phases; per-phase PRDs written to `implementationplan/phase-<n>-*.md`. All stories `ready: false, done: false`. Phase-1 through phase-11 own the foundation + onboarding wizard (B1–B10); phase-12 through phase-19 are thin post-onboarding wires (profile, discover, bookmarks, friend-requests, friends, chat, blocks, settings) with visual design deferred to their /implement-phase brainstorms. Only phase 1 story 1.10 (CI) is assigned to `backenddeveloper`; every other story is `frontenddeveloper`.
- 2026-07-19: architecture.md — resolved §17.22 (Urdu font: Noto Nastaliq Urdu, SIL OFL) and §17.26 (RTL reload UX: confirm-then-reload modal, then `I18nManager.forceRTL` + `Updates.reloadAsync()`). phase-1 PRD updated: story 1.1 pins the Noto font family and adds a `useLocalizedFontFamily()` hook; story 1.8 pins the confirm-then-reload flow and installs `expo-updates`, with tests for accept/cancel/same-locale paths. Both phase-1 gating blockers are now closed.
- 2026-07-20: /implement-phase 1 Step 0 brainstorm identified 9 gaps in phase-1 PRD (`phasebrainstorms/phase-1-foundation-brainstorm.md`). Resolutions applied: (1) Noto Nastaliq Urdu `.ttf` files pre-placed by user at `src/assets/fonts/` (Regular/Medium/SemiBold/Bold + OFL license) - story 1.1 assumes files present and fails cleanly if missing. (2) architecture.md §16 rewritten to adopt Expo SDK 56 (RN 0.85, React 19.2, TS 6.0.3, Node ≥ 20.19.4) with `npx expo install` as the version-resolution pattern instead of hardcoded pins. (3) Story 1.2 explicit about `templateimages/` migration + deletion. (4) Story 1.2 uses absolute path `C:\Users\syede\Claude-Master\muzzscreenshots\countrycodes.js`. (5) Story 1.3 enumerates baseline label keys. (6) Story 1.4 makes `Text`/`Heading` consume `useLocalizedFontFamily()`. (7) Story 1.8 uses React Native `Alert.alert` (not catalog Modal) for the confirm-then-reload dialog. (8) Coverage thresholds moved into story 1.1's `jest.config.js` criterion. (9) Auth-gate test tightened with concrete `queryByText` assertions across all three auth states.
- 2026-07-21: Story 1.1 complete (commit 8d330df). Expo/RN/TS scaffold with Expo SDK 56; src/theme/ verbatim from theme.md §14 with urdu font family extension; App.tsx blocks on 9 fonts (5 PlusJakartaSans + 4 NotoNastaliqUrdu); useLocalizedFontFamily() stub hook; jest.config.js with coverageThreshold; tsconfig strict mode + @types/jest; 50 tests all pass, coverage exceeds all thresholds. PR #73 opened at https://github.com/syedemz/knotify-frontend/pull/73.
- 2026-07-21: Story 1.2 complete (commit cdcb38f). Central registries scaffolded: src/config/env.ts (EXPO_PUBLIC_ENV + startup validation), backendConfig.dev.json + .prod.json (frontend-trimmed shape), src/config/images.ts (5-key onboarding registry), src/config/countries.ts (195 CountryEntry[] with dialCode), 13 options JSON files + typed index.ts loader, 5 PNGs migrated from templateimages/ to src/assets/images/onboarding/ and templateimages/ deleted; 38 new tests pass, full suite 88/88.
- 2026-07-21: Story 1.3 complete (commit 19362a6). src/labels/ scaffolded: labels.en.json + labels.ur.json (7 keys, full parity), labels.types.ts (Paths<T> recursive type derives LabelKey union — t('missing.key') is TS2345 compile error), index.ts exports t(key: LabelKey): string with getActiveLocale() stub (TODO(1.8)) and EN fallback; 14 parity+resolver tests pass, full suite 102/102.
- 2026-07-21: Story 1.4 complete. 13 catalog components in src/components/: Screen, Box, Row, Column, Spacer, Divider, Text, Heading, Button, IconButton, PillButton, TouchableArea, Chip. All use theme-token-key props (SpacingKey, RadiusKey, TextColor, BgColor, etc.), no style prop, no raw values. Text and Heading consume useLocalizedFontFamily(). TSDoc on every exported Props type. 13 test files at __tests__/components/ with 165 new tests; full suite 267/267 pass, coverage 86.95%/82.57%/92.06%/87.5% (all above threshold). src/components/index.ts barrel-exports all 13 components.
- 2026-07-21: Story 1.5 complete (commit 302ecb0). 10 input catalog components added: TextInput, PasswordInput, SearchInput, FormField, Select, Slider, Switch, Checkbox, RadioGroup, DatePicker. Installed @react-native-community/datetimepicker@9.1.0 and @react-native-community/slider@5.2.0 via npx expo install. FormField uses children composition pattern. DatePicker: iOS inline spinner (onValueChange), Android trigger+dialog (onChange). 10 test files, 107 new tests; full suite 374/374 pass, coverage 86.32%/85.41%/85.95%/86.13% (all above threshold). tsc --noEmit: zero errors.
- 2026-07-21: Story 1.6 complete. 17 catalog components added: Card, Section, ListRow, ListRowSelectable, EmptyState, LoadingState, ErrorState, Modal, BottomSheet, Toast, Snackbar, Avatar, Image, Icon, Badge, NotificationDot, Illustration. Installed expo-image, lucide-react-native, react-native-svg, @gorhom/bottom-sheet, react-native-reanimated, react-native-gesture-handler. BottomSheet wraps @gorhom/bottom-sheet; GestureHandlerRootView wiring deferred to first consuming screen (TODO documented in BottomSheet.tsx). Modal is RN Modal wrapped for general confirms/details (not the language-change Alert, which stays in story 1.8). 17 test files, 158 new tests; full suite 532/532 pass, coverage 89.36%/87.54%/90.21%/89.36% (all above threshold). tsc --noEmit: zero errors.
- 2026-07-21: Story 1.7 complete. services/ I/O boundary scaffolded: httpClient.ts (request<T> contract, JWT injection, 401 silent-refresh-then-retry, configurable timeout, typed ApiError), errors.ts (ApiError class + normalizeErrorResponse + toError), mocks/handlers.ts (MSW setupServer empty worker, started in App.tsx when EXPO_PUBLIC_API_MODE=mock), cognitoClient.ts (aws-amplify v6 wrapper: signUp/confirmSignUp/signIn/signOut/refreshSession; no feature imports aws-amplify directly), secureStorage.ts (expo-secure-store typed facade with SecureStorageKey enum), expoPush.ts (registerForPushNotifications: permission → token → POST /v1/push-tokens), appsyncClient.ts (skeleton getAppsyncClient() returns null, TODO(phase-17)). Installed: expo-secure-store, expo-notifications, expo-device (npx expo install), aws-amplify@6, msw@2 (npm install). 54 new tests in 3 files; full suite 586/586 pass, coverage 83.44%/84.9%/86.69%/83.36% (all above threshold). tsc --noEmit: zero errors. Decision: httpClient tests use jest.spyOn(globalThis, 'fetch') instead of MSW worker because msw/native pulls in rettime (ESM-only, incompatible with jest-expo CJS runtime); MSW worker still runs in-app at runtime. handlers.ts excluded from jest coverage collection for same reason.
- 2026-07-21: Story 1.8 complete.
- 2026-07-21: Story 1.9 complete (commit 7699f91). Navigation skeleton: src/navigation/ with RootNavigator (auth-gate §6.2), AuthStack (Login/ForgotPassword/ResetPassword placeholders), OnboardingStack (Page01-Page31 placeholders), AppTabs (Discover/Requests/Chat/Menu placeholders), linking.ts (empty config), types.ts (4 ParamList types). App.tsx updated: NavigationContainer + RootNavigator wired, GestureHandlerRootView + SafeAreaProvider added (fulfills TODO(app-root) from 1.6). 4 nav.tabs label keys added with Urdu parity. 21 new tests in 4 files; 648/648 pass, coverage 83.63%/83.89%/85.15%/83.62% (all above threshold). tsc: zero errors. state/ providers scaffolded: LanguageProvider (locale/setLocale, AsyncStorage persist under app.locale, device-locale default, confirm-then-reload via Alert.alert per §17.26: persist→forceRTL→reloadAsync on accept, no-op on cancel), QueryProvider (staleTime 5m, retry 2, gcTime 10m, mutation retry 0 per §7.3), AuthProvider (status/session/profileComplete/signIn/signOut/refresh per §7.2, silent-refresh on mount, signOut clears TanStack Query cache). Rewired: useLocalizedFontFamily now reads from @/labels getActiveLocale() (module-level store updated by LanguageProvider via setActiveLocale()); labels/index.ts exports setActiveLocale()/getActiveLocale() — no circular deps. App.tsx provider order: fonts→ThemeProvider→LanguageProvider→QueryProvider→AuthProvider→placeholder. Installed: expo-updates, @react-native-async-storage/async-storage (npx expo install), @tanstack/react-query@5, expo-localization. jest.config.js: added @react-native-async-storage/async-storage mock mapping. 41 new tests (3 files); full suite 627/627 pass, coverage 83.16%/83.74%/86.41%/83.13% (all above threshold). tsc --noEmit: zero errors.
- 2026-07-21: Story 1.10 complete — Phase 1 done. .github/workflows/ci.yml created: triggers on pull_request→development and pull_request→main; runs npm ci, npx tsc --noEmit, npx eslint src __tests__, npx jest --coverage (includes labels-parity test). ESLint 9 flat config at eslint.config.js: @typescript-eslint/parser@8 + @typescript-eslint/eslint-plugin@8 + eslint-plugin-react@7 + eslint-plugin-react-hooks@7; minimal rules, zero errors on existing codebase (16 warnings only). Coverage thresholds (lines 80 / branches 75 / functions 80 / statements 80) enforced by jest.config.js, surfaces as non-zero jest exit on failure. cicd.md updated with workflow path, four commands, and coverage table. 648/648 tests pass; all coverage thresholds met (83.63%/83.89%/85.15%/83.62%). tsc: zero errors. eslint: zero errors.
