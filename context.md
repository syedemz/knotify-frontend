# knotify-frontend — Context

## Elevator pitch
<1-2 sentences. Auto-populate from architecture.md after /create-plan, or user fills now.>

## Current phase
Not yet started. Awaiting `architecture.md` and `/create-plan`.

## Active blockers
None. Phase 1 gates §17.22 and §17.26 both resolved on 2026-07-19.

## Before shipping

Local auth mock mode is active while the AWS backend is unspun (to save cost during UI walkthrough). Before the first real production build, delete the mock scaffolding in this order:

1. **`src/config/env.ts`** — delete the `isMockAuth` field from the exported `env` object (currently keyed off the sentinel `cognito.userPoolId === 'eu-central-1_dummydummy'`).
2. **`src/services/auth/cognitoClient.ts`** — delete every `if (env.isMockAuth) { ... }` branch (currently in `ensureConfigured`, `signUp`, `confirmSignUp`, `signIn`, `signOut`, `refreshSession`) and delete the `makeMockTokens` helper.
3. **`src/config/backendConfig.dev.json`** — replace the dummy sentinel values (`eu-central-1_dummydummy`, `dummydummydummydummydummy1`, `dummy.example.invalid`, `dummy.appsync-api...`, `dummy.cloudfront.net`) with the real values emitted by the backend Terraform outputs. `env.ts` throws at import time on any `<FILL_*>` placeholder, so partial fills are safe.

After all three are done, `grep -r isMockAuth src/` should return zero hits and `grep -r dummydummy src/config/` should return zero hits. Add a CI grep gate at that time if you want a hard stop.

### Mock-only registration + profile pipeline (added phase 11, 2026-08-03)

Registration completion and the phase-12 profile pipeline are intentionally mock-only until the backend (Cognito + AWS API + DB) is stood up. Every temporary surface added by phases 11–12 is grep-able via `TODO(mock-only)`. Before the first real production build, remove them in this order:

1. **Wire up `custom:profile_complete` JWT claim decode in `src/state/auth/AuthProvider.tsx`.** Resolve the `TODO(phase-2)` in the `profileComplete` `useMemo` (lines ~122-129) — decode the ID token, read the `custom:profile_complete` claim, return the boolean. This is what actually unlocks AppTabs post-onboarding once real Cognito is in play.
2. **Verify Cognito refresh returns a fresh ID token with the updated claim.** `AuthProvider.refresh()` calls `cognitoClient.refreshSession()`. If Amplify caches the ID token instead of re-fetching from Cognito after the backend flips `profile_complete_verified`, the claim will stay stale — in which case we need a manual `/oauth2/token` call or a sign-out/sign-in round-trip after the final PATCH. Verify with a real Cognito user pool before removing the mock-only bypass.
3. **Delete `src/state/onboardingCompletion/`** (the `OnboardingCompletionProvider` and its context) and remove its mount from `App.tsx`.
4. **Delete the additive `dummyOnboardingComplete` check in `src/navigation/RootNavigator.tsx`** — restore to a pure `profileComplete`-driven gate.
5. **Delete the mock dispatch branch in `src/services/api/httpClient.ts`** (the `if (env.isMock) { … mockRequest … }` at the top of `request()`) and **delete `src/services/api/mocks/mockRequest.ts`**. Once removed, real HTTP client calls the real backend for every path. Note: this replaces the earlier MSW-based mock (see 2026-08-04 change) — MSW was retired on Hermes because `msw/native` depends on browser globals that would require fragile polyfills.
6. **Delete the secure-store snapshot logic in `Page31FaceCaptureScreen`** — the `dummy.profile` write and the `dummy.onboarding.complete` flag write on mock-submit success.
7. **Delete the secure-store read logic in the mock GET `/profile/me` handler** and the merge logic in the mock PATCH `/profile/me` handler.
8. **One-shot migration on next launch:** wipe secure-store keys `dummy.profile` and `dummy.onboarding.complete` so no stale local data lingers.
9. **Verify YES/NO backend casing.** Fields `father_retired`, `mother_retired` (TEXT columns per phase 7) and `has_children`, `move_abroad` (BOOL columns per phase 8, already coerced) are written as literal `"YES"` / `"NO"` (uppercase) in the draft. Confirm the real backend accepts this exact casing for the two TEXT columns, or update `src/config/options/yesNo.json` (single source of truth) and re-run affected screens.

After all of the above: `grep -r 'TODO(mock-only)' src/` should return zero hits.

## Follow-up tickets

- **Real face-in-oval bounds check on Page 31.** The frame processor in `Page31FaceCaptureScreen.tsx` currently passes `faces.length > 0` as `faceInsideOval` — i.e. "any face detected anywhere in the frame", not "face bounds intersect the oval". Auto-capture threshold is bumped to 150 frames (~5s @ 30fps) as a stop-gap so the manual shutter button remains the practical capture path. Proper fix: read face bounds from `detectFaces()`, translate camera-sensor coordinates → screen coordinates (accounting for preview scaling, letterboxing, and front-camera mirroring), and gate `onFrame(true)` on real intersection with the `FaceOvalOverlay`'s bounds. Verify on ≥2 device sizes before shipping. Revert `CONSECUTIVE_FRAMES_REQUIRED` back to 15 once implemented.

## Critical design decisions
None yet.

## Recent changes
- 2026-07-26: Story 8.3 complete (branch feat/phase-8-lifestyle-b7). Page22PartnersReligiousLevelScreen: identical layout to page 21 — vertical radio-picker over options.religiousLevel, writes partners_religious_level on tap, auto-advances to Page23MaritalStatusScreen from tap handler only (not useEffect). On re-visit, initializedRef guard rehydrates selected row from draft.fields.partners_religious_level without re-firing advance. Labels onboarding.partnersReligiousLevel.title added to EN+UR with full parity. OnboardingStack placeholder for page 22 replaced with real screen. 16 new tests; 1580 → 1596 pass. tsc: zero errors; eslint: zero errors. Tracking issue #100 closed.
- 2026-07-26: Story 8.4 complete (branch feat/phase-8-lifestyle-b7). Page23MaritalStatusScreen: reactive two-stage screen — Never Married tap atomically writes { marital_status, has_children: false } and advances; Divorced/Widowed tap reveals children sub-question via local state only (no draft write); Yes/No tap writes both fields atomically (has_children coerced from 'YES'/'NO' → boolean) and advances. Reset rule enforced: Never Married handler always writes has_children=false unconditionally. Re-visit reconstructs local UI state from draft.fields.marital_status + draft.fields.has_children; auto-advance never fires on mount (no useEffect advance). Labels onboarding.maritalStatus.{title,hasChildren.label} added to EN+UR with full parity. OnboardingStack placeholder for page 23 replaced with real screen. 47 new tests; 1596 → 1643 pass; coverage 88.96/85.43/88.53/90.2. tsc: zero errors; eslint: zero errors. Tracking issue #101 closed. Non-obvious decisions: (a) pendingBranchStatus is local state, never written to draft, so partial Divorced selection survives a back-nav within the session but is discarded on full re-visit; (b) has_children: false written for Never Married is a boolean, not a string — tsc enforces this via DraftFields typing; (c) re-visit with has_children=false correctly highlights NO (explicit false vs null distinction preserved).
- 2026-07-26: Story 8.5 complete (branch feat/phase-8-lifestyle-b7). Page24MoveAbroadScreen: single YES/NO radio picker — first visit initialises selected='YES' as a visual-only default (no draft write on mount); tap coerces to boolean (move_abroad: tapped==='YES'), writes draft, calls advance(25), navigates to Page25Preferences1Screen; re-visit with move_abroad=true lights YES, re-visit with move_abroad=false lights NO, auto-advance never fires from mount. Labels onboarding.moveAbroad.title added to EN+UR with full parity. OnboardingStack placeholder for page 24 replaced with real screen. 26 new tests; 1643 → 1669 pass. tsc: zero errors. Tracking issue #102 closed. Non-obvious decision: null/false distinction in rehydration is critical — null (no prior answer) maps to the YES pre-select without writing draft; false (explicit NO answer) maps to NO highlight. Both avoid auto-advance.
- 2026-07-26: Phase 8 (Lifestyle + marital status — pages 20-24) complete. All 5 stories (8.1-8.5) shipped: Page 20 marriage timeline (dual pick-one, first client-local, second updates 7-option marriageTime catalog; new PillPickOneGroup catalog component), Page 21 own religious level (vertical radio), Page 22 partner's religious level (identical layout to page 21), Page 23 marital status (reactive Divorced/Widowed → children Yes/No, atomic writes, symmetric reset rule for Never Married), Page 24 move abroad (visual-only YES pre-select, boolean coercion, null/false rehydration distinction). Tracking issues #98-#102 closed. PR #103 open (feat/phase-8-lifestyle-b7 → development). Non-obvious phase-wide patterns: (a) `has_children` and `move_abroad` are `boolean | null` in UserProfile, so yesNo taps coerce `tapped === 'YES'` at write time (never write raw strings — tsc rejects); (b) auto-advance always wired inside tap handlers, never useEffect on draft state — prevents back-nav rubber-banding; (c) initializedRef guard pattern reused across pages 21/22/23/24 to safely rehydrate UI state without triggering advance on mount; (d) preselect is visual-only (component useState init), never a mount-time draft write, so back-nav distinguishes "user explicitly answered" from "default preselect shown". 1528 → 1669 tests (+141); final coverage 88.96/85.43/88.53/90.2 (all above threshold).
- 2026-07-26: Story 9.1 complete (branch feat/phase-9-preferences-b8). Page25PersonalityTraitsScreen: 57-trait pill multi-select (from new src/config/options/preferences1.json) with 5-trait selection cap (6th tap shows Snackbar toast), Continue button enabled at N>=1 ("Select (N)" label), Skip button (ghost variant) advances without writing, writes preferences: { personalityTraits: [...] } to draft.fields.preferences on Continue tap only; auto-advances to Page27RelationScreen (skipping unused page 26 slot). On re-visit rehydrates pre-selected pills from draft.fields.preferences?.personalityTraits via initializedRef guard — no auto-advance on mount. Labels onboarding.personalityTraits.{title,subtitle,continueLabel,skip,capExceededToast,options.*} added to EN+UR with full parity (41 non-MBTI traits translated, 16 MBTI acronyms same in both languages). OnboardingStack placeholder for page 25 replaced with real screen; router updates 25 → 27 directly (no page 26 stop). options.test.ts updated to include preferences1 in ALL_KEYS. 40 new tests (36 Page25 screen wiring + 4 options registry); 1669 → 1709 pass; coverage 89.1/85.45/88.37/90.39 (all above threshold). tsc: zero errors; eslint: zero errors. Tracking issue #104 closed. Non-obvious decisions: (a) Text component has no style prop — subtitle margin handled via View wrapper; (b) Set<string> for selected state provides O(1) toggle/has semantics; (c) Skip uses Button variant="ghost" directly (not WizardFooter.onBack) because onBack renders the "Back" label, not "Skip".
- 2026-07-26: Story 9.2 complete (branch feat/phase-9-preferences-b8). Page27RelationScreen: vertical radio-picker over options.relation (6 items: Myself/Son/Daughter/Sibling/Friend/Ward), writes relation on tap, auto-advances to Page28PhotosScreen from tap handler only (not useEffect). On re-visit, initializedRef guard rehydrates selected row from draft.fields.relation without re-firing advance. Labels onboarding.relation.{title,options.*} added to EN+UR with full parity. OnboardingStack placeholder for page 27 replaced with real screen. 18 new tests; 1709 → 1727 pass. tsc: zero errors; eslint: zero errors (7 pre-existing warnings unchanged). Tracking issue #105 closed.
- 2026-07-26: Phase 9 (Preferences + relation — pages 25-27) complete. Both stories (9.1-9.2) shipped: Page 25 personality traits multi-select (57-trait pill grid, 5-trait cap with Snackbar, Continue+Skip, writes preferences: { personalityTraits: [...] } into existing JSONB field — no schema bump, router jumps 25 → 27), Page 27 relation picker (vertical ListRowSelectable, tap-writes-and-advances). Original story 9.2 (interests multi-select) dropped from scope per user 2026-07-26 decision; page 26 slot unused. Tracking issues #104-#105 closed. PR #106 open (feat/phase-9-preferences-b8 → development). Non-obvious phase-wide decisions: (a) preferences JSONB field storage shape `{ personalityTraits: string[] }` uses the existing UserProfileWritable.preferences slot without any draft schema changes — resolves the previously deferred §17.20 [Open]; (b) MBTI acronyms deliberately untranslated in Urdu because they are international technical codes; (c) Skip button uses Button variant="ghost" directly (not WizardFooter.onBack, which renders the "Back" label); (d) selection cap enforced via Set<string> local state with Snackbar toast on 6th tap; (e) initializedRef guard reused on both stories to prevent mount-time auto-advance on re-visit. 1669 → 1727 tests (+58); final coverage 89.1/85.45/88.37/90.39 (all above threshold).
- 2026-07-26: fix/page-25-personality-pill-layout — post-merge UI polish on Page25PersonalityTraitsScreen. Four fixes applied: (1) pill layout changed from fixed flexBasis:'47%' two-column grid to natural flex-wrap with gap:sm so pills size to content width; (2) emoji icons added to all 57 traits via new src/config/options/preferences1Icons.json (PillButton gains `icon?: string` + `borderVariant?: 'default'|'subtle'` props; backwards-compatible, PillPickOneGroup on Page 20 unaffected); (3) unselected pill borders softened to StyleSheet.hairlineWidth + border.default colour (borderVariant='subtle') matching Muzz reference screenshots; (4) Continue always enabled — at N=0 label reads "Continue" (new continueLabelEmpty label) and no draft write occurs; Skip button removed entirely. Labels: `skip` key removed from EN+UR, `continueLabelEmpty` added. Tests: 36 → 35 (removed disabled-at-0 + all Skip assertions; added 3 AC(v) "0-selections → no write, advance" tests). 1727 → 1726 total pass; tsc: zero errors; eslint: zero new warnings. PR opened fix/page-25-personality-pill-layout → development.
- 2026-08-03: Story 10.1 complete (bd8445b, branch feat/phase-10-photos-phone). Page28PhotosScreen: 6-tile flex-wrap photo grid; expo-image-picker (native module — full APK reinstall required on device 000323572000090 after merge); requestMediaLibraryPermission() added to permissions service; setPhotoPreviewUris() added to useOnboardingDraft hook; PhotoTile feature-local component (empty/filled/denied states, remove badge, body-press-to-replace); bg.overlay token added to theme; labels onboarding.photos.* added EN+UR; Page29PhoneScreen stub added; OnboardingStack wired to real Page28 + stub Page29. 37 new tests (16 PhotoTile + 21 Page28); 1726→1763 pass. tsc: zero errors. Known limitation: Android content:// URIs may not survive cold restart until backend photo upload (§17.14) ships.
- 2026-08-03: Story 10.2 complete (19f3e12, branch feat/phase-10-photos-phone). Page29PhoneScreen: real implementation replacing stub — dial-code chip (defaults from resident_country_code lookup, fallback +91), TextInput number-pad maxLength 15, CountryPicker modal override (same component as page 15), Continue gated by isValidPhone(), writes E.164 via canonicalizePhone() to draft.phone_number, advances to Page30FaceVerifyIntroScreen. libphonenumber-js installed (JS-only, no prebuild). Draft schemaVersion bumped 2→3 (DISCARD policy: v2 drafts wiped on load — QA concern #8). Note: PRD claimed schema was already at v3 from phase 9, but observed state was v2 (phase 9's personalityTraits change never bumped it); the bump-by-one from actual state is correct and DISCARD semantics still hold. isValidPhone + canonicalizePhone added to validationHelper.ts (// ── phone ── section). Labels onboarding.phone.* added EN+UR (7 keys, full parity). ~22 test fixtures updated to schemaVersion 3 + phone_number: null. 37 new tests (20 validationHelper + 16 Page29 screen wiring + 1 draftSchema); 1763→1800 pass. tsc: zero errors; eslint: zero errors. Tracking issue #42 closed.
- 2026-08-03: Phase 10 (Photos + phone — pages 28-29) complete. Both stories (10.1-10.2) shipped: Page 28 photos (6-tile grid + expo-image-picker native module + media-library permission + PhotoTile component with remove/replace UX) and Page 29 phone (libphonenumber-js validation + dial-code chip + CountryPicker override + E.164 canonicalization + phone_number draft field). Tracking issues #41-#42 closed. PR #108 open (feat/phase-10-photos-phone → development). Non-obvious phase-wide decisions: (a) expo-image-picker is a native module — merging PR #108 requires `npx expo run:android` full APK reinstall on device 000323572000090 (READ_MEDIA_IMAGES permission activates on first install of new APK); (b) libphonenumber-js is JS-only, no prebuild needed — a Metro reload picks up story 10.2 alone; (c) default dial code is +91 India (per user 2026-08-03 targeting decision, not +92 Pakistan as originally suggested); (d) dial-code chip reuses the same CountryPicker component from page 15 rather than duplicating; (e) phone_number stored E.164 canonical (`+919812345678`), not split into dial/national; (f) Android content:// URIs for photos may not survive cold restart until backend upload (§17.14) — known limitation, not a bug for v1. 1727 → 1800 tests (+73); coverage above threshold.
- 2026-08-03: Story 11.1 complete (7c14961, branch feat/phase-11-face-verify-b10). Page30FaceIntroScreen: face verification intro + camera permission gate using react-native-vision-camera@~4.7.3 + vision-camera-face-detector@~0.1.8 (both native modules — full APK reinstall required on device 000323572000090 after merge); requestCameraPermission() added to permissions service with explicit mapVisionCameraStatus helper mapping 'granted'→'granted', 'denied'→'denied', 'not-determined'→'undetermined', 'restricted'→'denied'; __mocks__/ added for both vision-camera packages; Page30 renders explainer + "Verify photo" button, escalates denial state from none→firstDeny→permanent (first deny shows Retry only; permanent deny adds "Open Settings" Linking.openSettings deep link + Snackbar toast reusing Page25 pattern); labels onboarding.faceIntro.* added EN+UR (8 keys, full parity); OnboardingStack Page30 placeholder replaced with real screen. 26 new tests (10 cameraPermission + 16 Page30); 1800→1826 pass. tsc: zero errors; eslint: zero errors. Tracking issue #43 closed.
- 2026-08-03: Story 11.2 complete (branch feat/phase-11-face-verify-b10). Page31FaceCaptureScreen: full-screen camera preview (react-native-vision-camera Camera + useFrameProcessor) with FaceOvalOverlay (border-radius transparent oval, pointerEvents none); useAutoCaptureController pure state machine (15 consecutive true frames → shouldCapture, useRef counter, no re-render per frame); manual shutter button always visible; 5-step mock-only submit sequence (buildPatchBody → PATCH /profile/me via MSW → SecureStore dummy.profile snapshot → markComplete → clear); 409 username regeneration+retry; 500/network error surface with retry; state-driven navigation (markComplete() triggers RootNavigator re-render, NO navigation.navigate call). OnboardingCompletionProvider: reads/writes dummy.onboarding.complete from expo-secure-store, loading guard prevents first-mount race, resilience on read errors, runtime sanity warn on complete===true+unauthenticated. buildPatchBody pure function: explicit allowlist, isRetry strips immutable fields, serializeSiblings camelCase→snake_case. RootNavigator: !profileComplete && !mockOnboardingComplete gate + loading guard extended to include onboardingCompletion.loading. App.tsx tree updated with OnboardingCompletionProvider wrapping NavigationContainer. OnboardingDraft schemaVersion 3→4 + faceSelfieUri: string|null; ~25 fixture files updated. Button component gains testID prop forwarded to Pressable. react-native-vision-camera mock gains useFrameProcessor. Labels onboarding.faceCapture.* added EN+UR (8 keys, full parity). 84 new tests (21 Page31 + 12 useAutoCaptureController + 11 buildPatchBody + 18 OnboardingCompletionProvider + 2 auth-gate + 20 existing fixture updates); 1826→1910 pass. tsc: zero errors; eslint: zero errors. Tracking issue #44 closed.
- 2026-08-03: Phase 11 (Face verification + final PATCH — pages 30-31) complete. Both stories (11.1-11.2) shipped: Page 30 face intro (vision-camera permission gate with escalating denial UX + OS-settings deep link + Snackbar toast) and Page 31 face capture (full-screen camera + FaceOvalOverlay + pure state machine auto-capture + always-visible manual shutter + 5-step mock-only submit + OnboardingCompletionProvider + additive RootNavigator gate). Tracking issues #43-#44 closed. PR #109 open (feat/phase-11-face-verify-b10 → development). **This phase intentionally does NOT integrate the real backend** — per user decision 2026-08-03 (concern #3 in QA/explanations.txt), the AuthProvider stub and `custom:profile_complete` JWT claim decode remain untouched; registration completion is bypassed client-side via `dummy.onboarding.complete` in expo-secure-store and an additive `mockOnboardingComplete` check in RootNavigator. The assembled PATCH body is snapshotted to `dummy.profile` so phase 12's MyProfileScreen can read real user-entered data instead of a fixture. Every mock-only surface marked `// TODO(mock-only): remove when real backend + JWT claim decode ship` — teardown is a 9-item checklist in `context.md → Before shipping → Mock-only registration + profile pipeline`. Non-obvious phase-wide decisions: (a) `react-native-vision-camera` + `vision-camera-face-detector` chosen over expo-camera (§17.24 [Open] closed) — both native modules, merging PR #109 requires `npx expo run:android` full APK reinstall on device 000323572000090 to activate CAMERA permission; (b) `useAutoCaptureController` state machine uses `useRef` counter, not `useState`, to avoid per-frame re-renders during face detection; (c) navigation swap on submit success is state-driven (`markComplete()` flips context → RootNavigator re-renders → AppTabs), NOT imperative — screen never calls `navigation.navigate('AppTabs')`; (d) `OnboardingCompletionProvider` mounted as a descendant of AuthProvider (required for `useAuth()` sanity warning); (e) runtime sanity warn only fires on `status === 'unauthenticated'`, not `'loading'`, to avoid spurious warnings on every cold launch for completed users; (f) `markComplete()` owns both the secure-store write AND the in-memory flip in one method to prevent inconsistent state; (g) OnboardingDraft schemaVersion bumped 3→4 (DISCARD policy) for the `faceSelfieUri` field, ~25 fixtures updated in one mechanical pass; (h) `buildPatchBody` retry test iterates all `isImmutable()` keys — future additions to the helper are covered without editing the test. 1800 → 1910 tests (+110); coverage above threshold.
- 2026-08-04: Story 12.1 complete (branch feat/phase-12-landing-profile). Dummy JSON fixtures + type declarations: added `assets/dummyfemale.json` (Aisha Khan, 27, Berlin DE, Product Designer, 2 siblings, 2 photos, 5 personalityTraits, gold tier) and `assets/dummyprofile.json` (Adnan Malik, 29, Mannheim DE, Software Engineer, 0 siblings, 1 photo, 1 personalityTrait "Night owl", __mutability_notes block). Added `src/types/DummySibling.ts`, `src/types/DummyFemaleProfile.ts`, `src/types/DummyOwnProfile.ts` — all extend `UserProfile` via `Omit<UserProfile, 'preferences'>` (necessary because the narrowed `preferences` type adds `| undefined` which conflicts with interface-extends assignability). Added `resolveJsonModule: true` to `tsconfig.json`. 32 new tests in `__tests__/assets/dummyFixtures.test.ts`; 1902 → 1934 pass. tsc: zero errors. Tracking issue #112 closed.
